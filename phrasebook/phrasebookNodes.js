//
// Copyright (c) 2006 by Conor O'Mahony.
// For enquiries, please email GubuSoft@GubuSoft.com.
// Please keep all copyright notices below.
// Original author of TreeView script is Marcelino Martins.
//
// This document includes the TreeView script.
// The TreeView script can be found at http://www.TreeView.net.
// The script is Copyright (c) 2006 by Conor O'Mahony.
//
// You can find general instructions for this file at www.treeview.net.
//

USETEXTLINKS = 1
STARTALLOPEN = 0
USEFRAMES = 0
USEICONS = 0
WRAPTEXT = 1
PRESERVESTATE = 1

foldersTree = gFld("<strong>The Java Programmer's Phrase Book [1101669 methods, 100 applications] @ Sat Jul 19 16:06:30 2008</strong>")
  foldersTree.treeID = "Phrasebook"
  aux1 = insFld(foldersTree, gFld("<strong>accept-*</strong>"))
    insDoc(aux1, gFld("These methods often have parameters. They very rarely are static or return field values, and rarely throw exceptions, write parameter values to fields or call themselves recursively."))
   aux2 = insFld(aux1, gFld("<strong>accept</strong>"))
     insDoc(aux2, gFld("These methods often have parameters. They very rarely are static, return field values or return parameter values, and rarely write to fields, create objects, throw exceptions, write parameter values to fields or call themselves recursively."))
  aux1 = insFld(foldersTree, gFld("<strong>add-*</strong>"))
    insDoc(aux1, gFld("These methods often have parameters. They very rarely return field values. The phrase appears in practically all applications."))
   aux2 = insFld(aux1, gFld("<strong>add</strong>"))
     insDoc(aux2, gFld("These methods often have parameters, call other methods with the same name, create objects and throw exceptions. They very rarely return field values."))
    aux2 = insFld(aux1, gFld("<strong>add-[noun]-*</strong>"))
      insDoc(aux2, gFld("These methods often have parameters and create objects. They very rarely return field values. The phrase appears in practically all applications."))
     aux3 = insFld(aux2, gFld("<strong>add-[noun]</strong>"))
       insDoc(aux3, gFld("These methods often have parameters. They rarely return field values. The phrase appears in most applications."))
      aux3 = insFld(aux2, gFld("<strong>add-[noun]-[noun]-*</strong>"))
        insDoc(aux3, gFld("These methods often have parameters. They very rarely return field values, and rarely call other methods with the same name or return parameter values. The phrase appears in most applications."))
       aux4 = insFld(aux3, gFld("<strong>add-[noun]-[noun]</strong>"))
         insDoc(aux4, gFld("These methods often have parameters. They very rarely return field values, and rarely return parameter values. The phrase appears in most applications."))
        aux4 = insFld(aux3, gFld("<strong>add-[noun]-[noun]-[noun]-*</strong>"))
          insDoc(aux4, gFld("These methods often have parameters. They very rarely return field values or call themselves recursively, and rarely call other methods with the same name, return parameter values or have multiple return points."))
         aux5 = insFld(aux4, gFld("<strong>add-[noun]-[noun]-[noun]</strong>"))
           insDoc(aux5, gFld("These methods often have parameters. They very rarely return field values or call themselves recursively, and rarely call other methods with the same name, write parameter values to fields, return parameter values or have multiple return points."))
      aux3 = insFld(aux2, gFld("<strong>add-[noun]-[preposition]-*</strong>"))
        insDoc(aux3, gFld("These methods often have parameters, contain loops, use local variables, create objects, do runtime type-checking or casting and have branches, and comparatively often are static and call themselves recursively. They never return field values."))
    aux2 = insFld(aux1, gFld("<strong>add-[adjective]-*</strong>"))
      insDoc(aux2, gFld("These methods often use local variables, throw exceptions and do runtime type-checking or casting. They very rarely return field values, and rarely call other methods with the same name."))
      aux3 = insFld(aux2, gFld("<strong>add-[adjective]-[noun]-*</strong>"))
        insDoc(aux3, gFld("These methods often use local variables, throw exceptions and do runtime type-checking or casting. They very rarely return field values, and rarely call other methods with the same name, write parameter values to fields or return parameter values."))
       aux4 = insFld(aux3, gFld("<strong>add-[adjective]-[noun]</strong>"))
         insDoc(aux4, gFld("These methods often use local variables, throw exceptions and do runtime type-checking or casting. They very rarely return field values, and rarely call other methods with the same name or write parameter values to fields."))
        aux4 = insFld(aux3, gFld("<strong>add-[adjective]-[noun]-[noun]-*</strong>"))
          insDoc(aux4, gFld("These methods very often throw exceptions and do runtime type-checking or casting, and often read from fields and use local variables. They very rarely return field values, and rarely call other methods with the same name, write parameter values to fields, return parameter values or have multiple return points."))
         aux5 = insFld(aux4, gFld("<strong>add-[adjective]-[noun]-[noun]</strong>"))
           insDoc(aux5, gFld("These methods very often throw exceptions, and often read from fields, use local variables and do runtime type-checking or casting. They very rarely return field values, and rarely call other methods with the same name, write parameter values to fields or return parameter values."))
    aux2 = insFld(aux1, gFld("<strong>add-to-*</strong>"))
      insDoc(aux2, gFld("These methods often have parameters, contain loops, use local variables, create objects, do runtime type-checking or casting and have branches. They very rarely return field values."))
      aux3 = insFld(aux2, gFld("<strong>add-to-[noun]-*</strong>"))
        insDoc(aux3, gFld("These methods often have parameters, contain loops, use local variables, do runtime type-checking or casting and have branches. They never return field values."))
    aux2 = insFld(aux1, gFld("<strong>add-[pronoun]-*</strong>"))
      insDoc(aux2, gFld("These methods very often contain loops, and often have parameters, use local variables, throw exceptions and have branches, and comparatively often call themselves recursively. They never return field values, and very rarely write parameter values to fields."))
    aux2 = insFld(aux1, gFld("<strong>add-[type]-*</strong>"))
      insDoc(aux2, gFld("These methods often have parameters, call other methods with the same name and create objects. They very rarely return field values, and rarely return parameter values."))
     aux3 = insFld(aux2, gFld("<strong>add-[type]</strong>"))
       insDoc(aux3, gFld("These methods often have parameters, call other methods with the same name and create objects. They very rarely return field values, and rarely return parameter values."))
  aux1 = insFld(foldersTree, gFld("<strong>append-*</strong>"))
    insDoc(aux1, gFld("These methods often have parameters, contain loops, use local variables and have branches, and comparatively often are static and return parameter values. They very rarely return field values."))
    aux2 = insFld(aux1, gFld("<strong>append-[noun]-*</strong>"))
      insDoc(aux2, gFld("These methods often have parameters, contain loops, use local variables, are static and have branches, and comparatively often return parameter values. They very rarely return field values."))
     aux3 = insFld(aux2, gFld("<strong>append-[noun]</strong>"))
       insDoc(aux3, gFld("These methods often have parameters, contain loops and have branches, and comparatively often are static and return parameter values. They very rarely return field values."))
  aux1 = insFld(foldersTree, gFld("<strong>build-*</strong>"))
    insDoc(aux1, gFld("These methods often contain loops, use local variables, create objects, do runtime type-checking or casting and have branches, and comparatively often are static and call themselves recursively. They rarely return field values."))
    aux2 = insFld(aux1, gFld("<strong>build-[noun]-*</strong>"))
      insDoc(aux2, gFld("These methods often contain loops, use local variables, create objects, do runtime type-checking or casting and have branches, and comparatively often are static and call themselves recursively. They rarely call other methods with the same name, write parameter values to fields or return field values."))
  aux1 = insFld(foldersTree, gFld("<strong>can-*</strong>"))
    insDoc(aux1, gFld("These methods often have branches and have multiple return points, and comparatively often are static. They very rarely write parameter values to fields, and rarely return void, write to fields, create objects, throw exceptions or return parameter values."))
  aux1 = insFld(foldersTree, gFld("<strong>check-*</strong>"))
    insDoc(aux1, gFld("These methods very often have branches, and often contain loops, use local variables, create objects, throw exceptions, do runtime type-checking or casting, are static and have multiple return points. They rarely write parameter values to fields or return field values. The phrase appears in most applications."))
    aux2 = insFld(aux1, gFld("<strong>check-[noun]-*</strong>"))
      insDoc(aux2, gFld("These methods very often have branches, and often contain loops, use local variables, create objects, throw exceptions, are static and have multiple return points. They rarely write parameter values to fields or return field values."))
     aux3 = insFld(aux2, gFld("<strong>check-[noun]</strong>"))
       insDoc(aux3, gFld("These methods very often have branches, and often contain loops, use local variables, create objects, throw exceptions, are static and have multiple return points. They rarely return field values."))
      aux3 = insFld(aux2, gFld("<strong>check-[noun]-[noun]-*</strong>"))
        insDoc(aux3, gFld("These methods very often have branches, and often contain loops, use local variables, create objects, throw exceptions, are static and have multiple return points. They rarely write parameter values to fields."))
       aux4 = insFld(aux3, gFld("<strong>check-[noun]-[noun]</strong>"))
         insDoc(aux4, gFld("These methods very often have branches, and often contain loops, use local variables, create objects, throw exceptions, are static and have multiple return points."))
    aux2 = insFld(aux1, gFld("<strong>check-[adjective]-*</strong>"))
      insDoc(aux2, gFld("These methods very often have branches, and often contain loops, use local variables, create objects, throw exceptions and have multiple return points, and comparatively often are static. They rarely call other methods with the same name, write parameter values to fields or return field values."))
      aux3 = insFld(aux2, gFld("<strong>check-[adjective]-[noun]-*</strong>"))
        insDoc(aux3, gFld("These methods very often have branches, and often contain loops, use local variables, create objects, throw exceptions, do runtime type-checking or casting, are static and have multiple return points, and comparatively often call themselves recursively. They rarely call other methods with the same name, write parameter values to fields or return field values."))
  aux1 = insFld(foldersTree, gFld("<strong>clear-*</strong>"))
    insDoc(aux1, gFld("These methods often return void, write to fields and contain loops. They very rarely write parameter values to fields, return field values or return parameter values, and rarely have parameters, call themselves recursively or have multiple return points. The phrase appears in most applications."))
   aux2 = insFld(aux1, gFld("<strong>clear</strong>"))
     insDoc(aux2, gFld("These methods often return void, write to fields, call other methods with the same name and contain loops. They never return field values or return parameter values, and very rarely call themselves recursively, and rarely have parameters, are static, write parameter values to fields or have multiple return points."))
    aux2 = insFld(aux1, gFld("<strong>clear-[noun]-*</strong>"))
      insDoc(aux2, gFld("These methods often return void and write to fields. They very rarely write parameter values to fields or return field values, and rarely return parameter values, call themselves recursively or have multiple return points."))
     aux3 = insFld(aux2, gFld("<strong>clear-[noun]</strong>"))
       insDoc(aux3, gFld("These methods often return void and write to fields. They never write parameter values to fields, and very rarely return field values, and rarely return parameter values, call themselves recursively or have multiple return points."))
  aux1 = insFld(foldersTree, gFld("<strong>clone-*</strong>"))
    insDoc(aux1, gFld("These methods often write to fields, call other methods with the same name, use local variables, create objects and do runtime type-checking or casting. They very rarely write parameter values to fields or return field values, and rarely return void or call themselves recursively."))
   aux2 = insFld(aux1, gFld("<strong>clone</strong>"))
     insDoc(aux2, gFld("These methods very often do runtime type-checking or casting, and often write to fields, call other methods with the same name, use local variables and create objects. They never return void, write parameter values to fields or return field values, and very rarely call themselves recursively, and rarely have parameters or are static."))
  aux1 = insFld(foldersTree, gFld("<strong>close-*</strong>"))
    insDoc(aux1, gFld("These methods often write to fields, call other methods with the same name and have branches. They very rarely write parameter values to fields, return field values or return parameter values, and rarely call themselves recursively. The phrase appears in most applications."))
   aux2 = insFld(aux1, gFld("<strong>close</strong>"))
     insDoc(aux2, gFld("These methods often return void, write to fields and call other methods with the same name. They never return field values or return parameter values, and very rarely write parameter values to fields, and rarely have parameters or call themselves recursively."))
    aux2 = insFld(aux1, gFld("<strong>close-[noun]-*</strong>"))
      insDoc(aux2, gFld("These methods often write to fields, use local variables and have branches, and comparatively often are static. They never write parameter values to fields, and very rarely return field values or return parameter values, and rarely call themselves recursively."))
     aux3 = insFld(aux2, gFld("<strong>close-[noun]</strong>"))
       insDoc(aux3, gFld("These methods often write to fields, use local variables and have branches, and comparatively often are static. They never write parameter values to fields or call themselves recursively, and very rarely return field values or return parameter values."))
  aux1 = insFld(foldersTree, gFld("<strong>compare-*</strong>"))
    insDoc(aux1, gFld("These methods very often have parameters, do runtime type-checking or casting and have multiple return points, and often use local variables and have branches. They rarely return void, write to fields, write parameter values to fields, return field values or return parameter values. The phrase appears in most applications."))
   aux2 = insFld(aux1, gFld("<strong>compare</strong>"))
     insDoc(aux2, gFld("These methods very often have parameters, do runtime type-checking or casting and have multiple return points, and often use local variables and have branches. They very rarely return void, write to fields, write parameter values to fields or return field values, and rarely return parameter values."))
    aux2 = insFld(aux1, gFld("<strong>compare-to-*</strong>"))
      insDoc(aux2, gFld("These methods always have parameters, and very often do runtime type-checking or casting and have multiple return points, and often call other methods with the same name, use local variables and have branches. They never return void, and very rarely write to fields, write parameter values to fields, return field values or return parameter values."))
     aux3 = insFld(aux2, gFld("<strong>compare-to</strong>"))
       insDoc(aux3, gFld("These methods always have parameters, and very often do runtime type-checking or casting and have multiple return points, and often call other methods with the same name, use local variables and have branches. They never return void, and very rarely write to fields, write parameter values to fields, return field values or return parameter values, and rarely are static or call themselves recursively."))
  aux1 = insFld(foldersTree, gFld("<strong>contains-*</strong>"))
    insDoc(aux1, gFld("These methods often have parameters, contain loops, have branches and have multiple return points. They never return void, and very rarely write to fields, write parameter values to fields, return field values or return parameter values, and rarely create objects."))
   aux2 = insFld(aux1, gFld("<strong>contains</strong>"))
     insDoc(aux2, gFld("These methods very often have parameters, and often contain loops, have branches and have multiple return points. They never return void, and very rarely write to fields, return field values or return parameter values, and rarely create objects, write parameter values to fields or call themselves recursively."))
    aux2 = insFld(aux1, gFld("<strong>contains-key-*</strong>"))
      insDoc(aux2, gFld("These methods often have parameters, call other methods with the same name, contain loops and have multiple return points. They never return void, return field values, return parameter values or call themselves recursively, and very rarely write to fields, are static or write parameter values to fields, and rarely create objects."))
     aux3 = insFld(aux2, gFld("<strong>contains-key</strong>"))
       insDoc(aux3, gFld("These methods always have parameters, and often call other methods with the same name, contain loops and have multiple return points. They never return void, return field values, return parameter values or call themselves recursively, and very rarely write to fields, are static or write parameter values to fields, and rarely create objects."))
    aux2 = insFld(aux1, gFld("<strong>contains-[/noun]-*</strong>"))
      insDoc(aux2, gFld("These methods often have parameters, contain loops, have branches and have multiple return points. They never return void or write parameter values to fields, and very rarely write to fields or return parameter values, and rarely return field values."))
  aux1 = insFld(foldersTree, gFld("<strong>convert-*</strong>"))
    insDoc(aux1, gFld("These methods often have parameters, contain loops, use local variables, create objects, do runtime type-checking or casting, are static, have branches and have multiple return points, and comparatively often return parameter values. They rarely write parameter values to fields."))
    aux2 = insFld(aux1, gFld("<strong>convert-[noun]-*</strong>"))
      insDoc(aux2, gFld("These methods very often return parameter values and have branches, and often have parameters, contain loops, use local variables, create objects, do runtime type-checking or casting, are static and have multiple return points. They very rarely write parameter values to fields."))
  aux1 = insFld(foldersTree, gFld("<strong>copy-*</strong>"))
    insDoc(aux1, gFld("These methods often contain loops, use local variables, create objects, do runtime type-checking or casting, are static and have branches, and comparatively often call themselves recursively. They very rarely return field values."))
   aux2 = insFld(aux1, gFld("<strong>copy</strong>"))
     insDoc(aux2, gFld("These methods often contain loops, use local variables, create objects, do runtime type-checking or casting and are static. They very rarely return field values, and rarely return parameter values."))
    aux2 = insFld(aux1, gFld("<strong>copy-[noun]-*</strong>"))
      insDoc(aux2, gFld("These methods very often contain loops, and often have parameters, use local variables, do runtime type-checking or casting, are static and have branches, and comparatively often return parameter values and call themselves recursively. They very rarely return field values, and rarely write parameter values to fields."))
  aux1 = insFld(foldersTree, gFld("<strong>create-*</strong>"))
    insDoc(aux1, gFld("These methods very often create objects, and often use local variables and are static. They rarely write parameter values to fields, return field values, return parameter values or call themselves recursively. The phrase appears in most applications."))
   aux2 = insFld(aux1, gFld("<strong>create</strong>"))
     insDoc(aux2, gFld("These methods very often create objects, and often are static. They very rarely return parameter values, and rarely call themselves recursively."))
    aux2 = insFld(aux1, gFld("<strong>create-[noun]-*</strong>"))
      insDoc(aux2, gFld("These methods very often create objects, and often use local variables and are static. They rarely return field values or call themselves recursively. The phrase appears in most applications."))
     aux3 = insFld(aux2, gFld("<strong>create-[noun]</strong>"))
       insDoc(aux3, gFld("These methods very often create objects, and often use local variables and are static. They rarely call themselves recursively. The phrase appears in most applications."))
      aux3 = insFld(aux2, gFld("<strong>create-[noun]-[noun]-*</strong>"))
        insDoc(aux3, gFld("These methods very often create objects, and often use local variables and are static. They rarely write parameter values to fields, return field values or call themselves recursively."))
       aux4 = insFld(aux3, gFld("<strong>create-[noun]-[noun]</strong>"))
         insDoc(aux4, gFld("These methods very often create objects, and often use local variables and are static. They rarely write parameter values to fields or call themselves recursively."))
      aux3 = insFld(aux2, gFld("<strong>create-[noun]-[type]-*</strong>"))
        insDoc(aux3, gFld("These methods often use local variables, create objects, do runtime type-checking or casting and are static. They rarely return field values, return parameter values or call themselves recursively."))
       aux4 = insFld(aux3, gFld("<strong>create-[noun]-[type]</strong>"))
         insDoc(aux4, gFld("These methods often use local variables, create objects and are static. They rarely return field values, return parameter values or call themselves recursively."))
    aux2 = insFld(aux1, gFld("<strong>create-[adjective]-*</strong>"))
      insDoc(aux2, gFld("These methods very often create objects, and often use local variables and are static. They rarely return field values."))
      aux3 = insFld(aux2, gFld("<strong>create-[adjective]-[noun]-*</strong>"))
        insDoc(aux3, gFld("These methods very often create objects, and often use local variables and are static. They rarely write parameter values to fields, return field values or return parameter values."))
       aux4 = insFld(aux3, gFld("<strong>create-[adjective]-[noun]</strong>"))
         insDoc(aux4, gFld("These methods very often create objects, and comparatively often are static. They rarely return field values or return parameter values."))
      aux3 = insFld(aux2, gFld("<strong>create-[adjective]-[type]-*</strong>"))
        insDoc(aux3, gFld("These methods very often create objects, and often use local variables and are static. They never call themselves recursively, and rarely call other methods with the same name or return field values."))
       aux4 = insFld(aux3, gFld("<strong>create-[adjective]-[type]</strong>"))
         insDoc(aux4, gFld("These methods very often create objects, and often use local variables and are static. They never call themselves recursively, and rarely return field values."))
    aux2 = insFld(aux1, gFld("<strong>create-[type]-*</strong>"))
      insDoc(aux2, gFld("These methods very often create objects, and often use local variables, and comparatively often are static. They very rarely return parameter values, and rarely write parameter values to fields, return field values or call themselves recursively. The phrase appears in most applications."))
     aux3 = insFld(aux2, gFld("<strong>create-[type]</strong>"))
       insDoc(aux3, gFld("These methods very often create objects, and comparatively often are static. They very rarely return parameter values, and rarely write parameter values to fields, return field values or call themselves recursively. The phrase appears in most applications."))
  aux1 = insFld(foldersTree, gFld("<strong>delete-*</strong>"))
    insDoc(aux1, gFld("These methods often contain loops, use local variables, create objects, throw exceptions and have branches, and comparatively often are static and call themselves recursively. They very rarely return field values, and rarely write parameter values to fields."))
    aux2 = insFld(aux1, gFld("<strong>delete-[noun]-*</strong>"))
      insDoc(aux2, gFld("These methods often contain loops, use local variables, create objects, throw exceptions, do runtime type-checking or casting and have branches, and comparatively often are static and call themselves recursively. They very rarely return field values, and rarely write parameter values to fields or return parameter values."))
     aux3 = insFld(aux2, gFld("<strong>delete-[noun]</strong>"))
       insDoc(aux3, gFld("These methods often contain loops, use local variables, create objects, throw exceptions and have branches, and comparatively often are static and call themselves recursively. They rarely write parameter values to fields, return field values or return parameter values."))
  aux1 = insFld(foldersTree, gFld("<strong>do-*</strong>"))
    insDoc(aux1, gFld("These methods often contain loops, use local variables, create objects, throw exceptions, do runtime type-checking or casting and have branches. They rarely return field values."))
    aux2 = insFld(aux1, gFld("<strong>do-[verb]-*</strong>"))
      insDoc(aux2, gFld("These methods often contain loops, use local variables, create objects, throw exceptions, do runtime type-checking or casting and have branches. They rarely return field values."))
    aux2 = insFld(aux1, gFld("<strong>do-[noun]-*</strong>"))
      insDoc(aux2, gFld("These methods often contain loops, use local variables, create objects, throw exceptions, do runtime type-checking or casting and have branches. They rarely return field values or return parameter values."))
     aux3 = insFld(aux2, gFld("<strong>do-[noun]</strong>"))
       insDoc(aux3, gFld("These methods often contain loops, use local variables, create objects, do runtime type-checking or casting and have branches. They rarely return field values or return parameter values."))
  aux1 = insFld(foldersTree, gFld("<strong>dump-*</strong>"))
    insDoc(aux1, gFld("These methods very often use local variables and create objects, and often contain loops. They very rarely write parameter values to fields, return field values or return parameter values, and rarely write to fields, throw exceptions or have multiple return points."))
  aux1 = insFld(foldersTree, gFld("<strong>end-*</strong>"))
    insDoc(aux1, gFld("These methods very rarely call themselves recursively, and rarely are static, write parameter values to fields, return field values or return parameter values."))
    aux2 = insFld(aux1, gFld("<strong>end-[noun]-*</strong>"))
      insDoc(aux2, gFld("These methods often return void. They very rarely call themselves recursively, and rarely are static, write parameter values to fields, return field values or return parameter values."))
     aux3 = insFld(aux2, gFld("<strong>end-[noun]</strong>"))
       insDoc(aux3, gFld("These methods often return void. They very rarely are static, and rarely write parameter values to fields, return field values, return parameter values or call themselves recursively."))
  aux1 = insFld(foldersTree, gFld("<strong>equals-*</strong>"))
    insDoc(aux1, gFld("These methods very often have parameters, call other methods with the same name, do runtime type-checking or casting, have branches and have multiple return points, and often use local variables. They never return field values or return parameter values, and very rarely return void, write to fields or write parameter values to fields, and rarely create objects, throw exceptions or call themselves recursively. The phrase appears in most applications."))
   aux2 = insFld(aux1, gFld("<strong>equals</strong>"))
     insDoc(aux2, gFld("These methods very often have parameters, call other methods with the same name, do runtime type-checking or casting, have branches and have multiple return points, and often use local variables. They never return field values or return parameter values, and very rarely return void, write to fields, write parameter values to fields or call themselves recursively, and rarely create objects or throw exceptions. The phrase appears in most applications."))
  aux1 = insFld(foldersTree, gFld("<strong>execute-*</strong>"))
    insDoc(aux1, gFld("These methods often contain loops, use local variables, create objects, throw exceptions, do runtime type-checking or casting and have branches. They rarely return parameter values or call themselves recursively."))
   aux2 = insFld(aux1, gFld("<strong>execute</strong>"))
     insDoc(aux2, gFld("These methods often read from fields, contain loops, use local variables, create objects, throw exceptions, do runtime type-checking or casting, have branches and have multiple return points. They very rarely return parameter values, and rarely are static or call themselves recursively."))
  aux1 = insFld(foldersTree, gFld("<strong>extract-*</strong>"))
    insDoc(aux1, gFld("These methods very often are static, and often have parameters. They rarely call other methods with the same name, write parameter values to fields, return field values or call themselves recursively."))
    aux2 = insFld(aux1, gFld("<strong>extract-[noun]-*</strong>"))
      insDoc(aux2, gFld("These methods very often use local variables, and often have parameters, contain loops, create objects, do runtime type-checking or casting, are static, have branches and have multiple return points, and comparatively often return parameter values. They rarely call other methods with the same name or write parameter values to fields."))
  aux1 = insFld(foldersTree, gFld("<strong>find-*</strong>"))
    insDoc(aux1, gFld("These methods very often contain loops, use local variables and have multiple return points, and often do runtime type-checking or casting, are static and have branches, and comparatively often return parameter values and call themselves recursively. They rarely write parameter values to fields. The phrase appears in most applications."))
    aux2 = insFld(aux1, gFld("<strong>find-[noun]-*</strong>"))
      insDoc(aux2, gFld("These methods very often contain loops and have multiple return points, and often use local variables, create objects, do runtime type-checking or casting, are static and have branches, and comparatively often return parameter values and call themselves recursively. They rarely write parameter values to fields."))
     aux3 = insFld(aux2, gFld("<strong>find-[noun]</strong>"))
       insDoc(aux3, gFld("These methods very often contain loops and have multiple return points, and often have parameters, use local variables, create objects, do runtime type-checking or casting, are static and have branches, and comparatively often return parameter values and call themselves recursively. They rarely return void or write parameter values to fields."))
      aux3 = insFld(aux2, gFld("<strong>find-[noun]-[noun]-*</strong>"))
        insDoc(aux3, gFld("These methods very often contain loops, and often use local variables, throw exceptions, do runtime type-checking or casting, are static, have branches and have multiple return points, and comparatively often call themselves recursively. They very rarely write parameter values to fields."))
       aux4 = insFld(aux3, gFld("<strong>find-[noun]-[noun]</strong>"))
         insDoc(aux4, gFld("These methods very often contain loops, and often use local variables, throw exceptions, do runtime type-checking or casting, are static, have branches and have multiple return points, and comparatively often call themselves recursively. They very rarely write parameter values to fields."))
    aux2 = insFld(aux1, gFld("<strong>find-[adjective]-*</strong>"))
      insDoc(aux2, gFld("These methods very often contain loops, use local variables, have branches and have multiple return points, and often do runtime type-checking or casting and are static, and comparatively often return parameter values and call themselves recursively."))
      aux3 = insFld(aux2, gFld("<strong>find-[adjective]-[noun]-*</strong>"))
        insDoc(aux3, gFld("These methods very often contain loops, use local variables, have branches and have multiple return points, and often do runtime type-checking or casting and are static, and comparatively often return parameter values and call themselves recursively. They rarely write parameter values to fields."))
       aux4 = insFld(aux3, gFld("<strong>find-[adjective]-[noun]</strong>"))
         insDoc(aux4, gFld("These methods very often contain loops, use local variables, have branches and have multiple return points, and often do runtime type-checking or casting and are static, and comparatively often return parameter values and call themselves recursively. They rarely write parameter values to fields or return field values."))
    aux2 = insFld(aux1, gFld("<strong>find-[type]-*</strong>"))
      insDoc(aux2, gFld("These methods very often contain loops, use local variables, have branches and have multiple return points, and often throw exceptions, do runtime type-checking or casting and are static, and comparatively often call themselves recursively. They rarely return void or write parameter values to fields."))
     aux3 = insFld(aux2, gFld("<strong>find-[type]</strong>"))
       insDoc(aux3, gFld("These methods very often contain loops, use local variables, have branches and have multiple return points, and often throw exceptions, do runtime type-checking or casting and are static, and comparatively often call themselves recursively. They rarely return void or write parameter values to fields."))
  aux1 = insFld(foldersTree, gFld("<strong>generate-*</strong>"))
    insDoc(aux1, gFld("These methods often contain loops, use local variables, create objects, do runtime type-checking or casting and have branches, and comparatively often are static. They rarely return field values."))
    aux2 = insFld(aux1, gFld("<strong>generate-[noun]-*</strong>"))
      insDoc(aux2, gFld("These methods often contain loops, use local variables, create objects, do runtime type-checking or casting, have branches and have multiple return points, and comparatively often are static. They rarely return field values."))
     aux3 = insFld(aux2, gFld("<strong>generate-[noun]</strong>"))
       insDoc(aux3, gFld("These methods often contain loops, use local variables, create objects, do runtime type-checking or casting, have branches and have multiple return points, and comparatively often are static. They rarely return field values or return parameter values."))
  aux1 = insFld(foldersTree, gFld("<strong>get-*</strong>"))
    insDoc(aux1, gFld("These methods very rarely return void or write parameter values to fields, and rarely call themselves recursively. The phrase appears in all applications."))
   aux2 = insFld(aux1, gFld("<strong>get</strong>"))
     insDoc(aux2, gFld("These methods often call other methods with the same name, use local variables, do runtime type-checking or casting, have branches and have multiple return points, and comparatively often are static. They very rarely write parameter values to fields, and rarely return void or call themselves recursively."))
    aux2 = insFld(aux1, gFld("<strong>get-[verb]-*</strong>"))
      insDoc(aux2, gFld("These methods often return field values. They very rarely return void, write parameter values to fields or call themselves recursively, and rarely write to fields or return parameter values."))
      aux3 = insFld(aux2, gFld("<strong>get-[verb]-[noun]-*</strong>"))
        insDoc(aux3, gFld("These methods very rarely return void, write parameter values to fields or call themselves recursively, and rarely write to fields or return parameter values."))
       aux4 = insFld(aux3, gFld("<strong>get-[verb]-[noun]</strong>"))
         insDoc(aux4, gFld("These methods often return field values. They very rarely write parameter values to fields or call themselves recursively, and rarely return void, write to fields or return parameter values."))
    aux2 = insFld(aux1, gFld("<strong>get-attribute-*</strong>"))
      insDoc(aux2, gFld("These methods often throw exceptions and have multiple return points. They very rarely return void or write parameter values to fields, and rarely write to fields or call themselves recursively."))
     aux3 = insFld(aux2, gFld("<strong>get-attribute</strong>"))
       insDoc(aux3, gFld("These methods often have parameters, call other methods with the same name and have multiple return points. They very rarely return void, and rarely write to fields or write parameter values to fields."))
    aux2 = insFld(aux1, gFld("<strong>get-child-*</strong>"))
      insDoc(aux2, gFld("These methods often contain loops, do runtime type-checking or casting and have multiple return points, and comparatively often are static. They never write parameter values to fields, and rarely return void."))
    aux2 = insFld(aux1, gFld("<strong>get-class-*</strong>"))
      insDoc(aux2, gFld("These methods comparatively often are static. They never write parameter values to fields, and very rarely return void, and rarely write to fields or call themselves recursively."))
      aux3 = insFld(aux2, gFld("<strong>get-class-name-*</strong>"))
        insDoc(aux3, gFld("These methods often return field values. They never return void or write parameter values to fields, and rarely write to fields, call other methods with the same name, contain loops, use local variables, create objects, throw exceptions, do runtime type-checking or casting, call themselves recursively or have branches."))
    aux2 = insFld(aux1, gFld("<strong>get-column-*</strong>"))
      insDoc(aux2, gFld("These methods very rarely return void, write parameter values to fields or call themselves recursively, and rarely write to fields or return parameter values."))
    aux2 = insFld(aux1, gFld("<strong>get-data-*</strong>"))
      insDoc(aux2, gFld("These methods very rarely write parameter values to fields, and rarely return void or call themselves recursively."))
    aux2 = insFld(aux1, gFld("<strong>get-end-*</strong>"))
      insDoc(aux2, gFld("These methods often read from fields and return field values. They never write parameter values to fields or call themselves recursively, and rarely return void, write to fields, contain loops, use local variables, are static or return parameter values."))
    aux2 = insFld(aux1, gFld("<strong>get-error-*</strong>"))
      insDoc(aux2, gFld("These methods often return field values. They never return void, write parameter values to fields, return parameter values or call themselves recursively, and rarely write to fields, call other methods with the same name or throw exceptions."))
      aux3 = insFld(aux2, gFld("<strong>get-error-[noun]-*</strong>"))
        insDoc(aux3, gFld("These methods often return field values. They never return void, write parameter values to fields, return parameter values or call themselves recursively, and rarely write to fields, call other methods with the same name or throw exceptions."))
       aux4 = insFld(aux3, gFld("<strong>get-error-[noun]</strong>"))
         insDoc(aux4, gFld("These methods often return field values. They never return void, write parameter values to fields, return parameter values or call themselves recursively, and rarely write to fields, call other methods with the same name, use local variables or throw exceptions."))
    aux2 = insFld(aux1, gFld("<strong>get-file-*</strong>"))
      insDoc(aux2, gFld("These methods comparatively often are static. They very rarely return void or write parameter values to fields, and rarely write to fields or call themselves recursively."))
      aux3 = insFld(aux2, gFld("<strong>get-file-[noun]-*</strong>"))
        insDoc(aux3, gFld("These methods comparatively often are static. They very rarely return void or write parameter values to fields, and rarely write to fields or call themselves recursively."))
    aux2 = insFld(aux1, gFld("<strong>get-id-*</strong>"))
      insDoc(aux2, gFld("These methods never write parameter values to fields or call themselves recursively, and very rarely return void, contain loops or return parameter values, and rarely have parameters, write to fields, call other methods with the same name, create objects or are static."))
     aux3 = insFld(aux2, gFld("<strong>get-id</strong>"))
       insDoc(aux3, gFld("These methods never write parameter values to fields or call themselves recursively, and very rarely return void, contain loops, are static or return parameter values, and rarely have parameters, write to fields, call other methods with the same name or create objects."))
    aux2 = insFld(aux1, gFld("<strong>get-index-*</strong>"))
      insDoc(aux2, gFld("These methods often read from fields, contain loops and have multiple return points. They never write parameter values to fields, and very rarely return void or call themselves recursively, and rarely write to fields."))
     aux3 = insFld(aux2, gFld("<strong>get-index</strong>"))
       insDoc(aux3, gFld("These methods often read from fields, contain loops, return field values and have multiple return points. They never return void or write parameter values to fields, and very rarely write to fields, and rarely create objects, throw exceptions, return parameter values or call themselves recursively."))
    aux2 = insFld(aux1, gFld("<strong>get-instance-*</strong>"))
      insDoc(aux2, gFld("These methods very often are static, and often create objects, throw exceptions, do runtime type-checking or casting and return field values. They never return void, and rarely write to fields, write parameter values to fields or return parameter values."))
     aux3 = insFld(aux2, gFld("<strong>get-instance</strong>"))
       insDoc(aux3, gFld("These methods very often are static, and often create objects, throw exceptions, do runtime type-checking or casting and return field values. They never return void, and rarely write to fields, contain loops, write parameter values to fields or return parameter values."))
    aux2 = insFld(aux1, gFld("<strong>get-int-*</strong>"))
      insDoc(aux2, gFld("These methods often are static and have multiple return points, and comparatively often return parameter values. They never return void, write parameter values to fields or call themselves recursively, and rarely write to fields."))
    aux2 = insFld(aux1, gFld("<strong>get-key-*</strong>"))
      insDoc(aux2, gFld("These methods often return field values. They very rarely return void, write parameter values to fields or call themselves recursively, and rarely write to fields."))
     aux3 = insFld(aux2, gFld("<strong>get-key</strong>"))
       insDoc(aux3, gFld("These methods often read from fields and return field values. They never return void, and rarely write to fields, use local variables, write parameter values to fields or call themselves recursively."))
    aux2 = insFld(aux1, gFld("<strong>get-length-*</strong>"))
      insDoc(aux2, gFld("These methods often read from fields. They never return void or write parameter values to fields, and very rarely call themselves recursively, and rarely have parameters, write to fields, create objects, throw exceptions, do runtime type-checking or casting or return parameter values."))
     aux3 = insFld(aux2, gFld("<strong>get-length</strong>"))
       insDoc(aux3, gFld("These methods often read from fields. They never return void or write parameter values to fields, and very rarely create objects, and rarely have parameters, use local variables, throw exceptions, do runtime type-checking or casting, are static, return parameter values or call themselves recursively."))
    aux2 = insFld(aux1, gFld("<strong>get-line-*</strong>"))
      insDoc(aux2, gFld("These methods often read from fields. They never call themselves recursively, and very rarely return void or write parameter values to fields, and rarely write to fields."))
    aux2 = insFld(aux1, gFld("<strong>get-max-*</strong>"))
      insDoc(aux2, gFld("These methods often return field values. They never write parameter values to fields, and very rarely return void, write to fields, return parameter values or call themselves recursively, and rarely have parameters, use local variables or create objects."))
      aux3 = insFld(aux2, gFld("<strong>get-max-[noun]-*</strong>"))
        insDoc(aux3, gFld("These methods often return field values. They never write parameter values to fields, and very rarely return void, write to fields, return parameter values or call themselves recursively, and rarely have parameters, create objects or have multiple return points."))
       aux4 = insFld(aux3, gFld("<strong>get-max-[noun]</strong>"))
         insDoc(aux4, gFld("These methods often read from fields and return field values. They never return void, write parameter values to fields, return parameter values or call themselves recursively, and rarely have parameters, write to fields or create objects."))
    aux2 = insFld(aux1, gFld("<strong>get-message-*</strong>"))
      insDoc(aux2, gFld("These methods comparatively often are static. They never write parameter values to fields, and very rarely return void, and rarely write to fields or call themselves recursively."))
     aux3 = insFld(aux2, gFld("<strong>get-message</strong>"))
       insDoc(aux3, gFld("These methods often call other methods with the same name, are static and have multiple return points. They never write parameter values to fields, and very rarely return void, and rarely write to fields or call themselves recursively."))
    aux2 = insFld(aux1, gFld("<strong>get-method-*</strong>"))
      insDoc(aux2, gFld("These methods often do runtime type-checking or casting, and comparatively often are static. They very rarely write parameter values to fields or return parameter values, and rarely return void, write to fields, call other methods with the same name or call themselves recursively."))
    aux2 = insFld(aux1, gFld("<strong>get-name-*</strong>"))
      insDoc(aux2, gFld("These methods often return field values. They never write parameter values to fields, and very rarely call themselves recursively, and rarely have parameters, return void, write to fields, contain loops, use local variables, create objects, throw exceptions, do runtime type-checking or casting, are static or return parameter values. The phrase appears in most applications."))
     aux3 = insFld(aux2, gFld("<strong>get-name</strong>"))
       insDoc(aux3, gFld("These methods often return field values. They never write parameter values to fields, and very rarely contain loops or call themselves recursively, and rarely have parameters, return void, write to fields, use local variables, create objects, throw exceptions, do runtime type-checking or casting, are static, return parameter values or have branches. The phrase appears in most applications."))
    aux2 = insFld(aux1, gFld("<strong>get-parameter-*</strong>"))
      insDoc(aux2, gFld("These methods often do runtime type-checking or casting and have multiple return points. They never write parameter values to fields or call themselves recursively, and very rarely return void."))
      aux3 = insFld(aux2, gFld("<strong>get-parameter-[noun]-*</strong>"))
        insDoc(aux3, gFld("These methods often have multiple return points, and comparatively often contain loops. They never return void, write parameter values to fields, return parameter values or call themselves recursively."))
    aux2 = insFld(aux1, gFld("<strong>get-parent-*</strong>"))
      insDoc(aux2, gFld("These methods never write parameter values to fields, and very rarely return void, and rarely write to fields, throw exceptions, return parameter values or call themselves recursively."))
     aux3 = insFld(aux2, gFld("<strong>get-parent</strong>"))
       insDoc(aux3, gFld("These methods often return field values. They never return void, write parameter values to fields or call themselves recursively, and rarely write to fields, contain loops, use local variables, create objects, throw exceptions or return parameter values."))
    aux2 = insFld(aux1, gFld("<strong>get-path-*</strong>"))
      insDoc(aux2, gFld("These methods never write parameter values to fields, and rarely return void or write to fields."))
    aux2 = insFld(aux1, gFld("<strong>get-property-*</strong>"))
      insDoc(aux2, gFld("These methods often have multiple return points. They never call themselves recursively, and very rarely return void or write parameter values to fields."))
     aux3 = insFld(aux2, gFld("<strong>get-property</strong>"))
       insDoc(aux3, gFld("These methods often have parameters, call other methods with the same name and have multiple return points, and comparatively often are static. They never return void or call themselves recursively, and very rarely write parameter values to fields."))
    aux2 = insFld(aux1, gFld("<strong>get-resource-*</strong>"))
      insDoc(aux2, gFld("These methods often do runtime type-checking or casting, are static and have multiple return points. They never write parameter values to fields, and very rarely return void, and rarely write to fields or call themselves recursively."))
    aux2 = insFld(aux1, gFld("<strong>get-root-*</strong>"))
      insDoc(aux2, gFld("These methods often return field values, and comparatively often contain loops and return parameter values. They never write parameter values to fields, and rarely return void."))
    aux2 = insFld(aux1, gFld("<strong>get-size-*</strong>"))
      insDoc(aux2, gFld("These methods often read from fields. They never return void, write parameter values to fields or call themselves recursively, and rarely have parameters or return parameter values."))
     aux3 = insFld(aux2, gFld("<strong>get-size</strong>"))
       insDoc(aux3, gFld("These methods often read from fields. They never return void, write parameter values to fields or call themselves recursively, and rarely have parameters, are static or return parameter values."))
    aux2 = insFld(aux1, gFld("<strong>get-source-*</strong>"))
      insDoc(aux2, gFld("These methods often return field values. They never write parameter values to fields, and very rarely return void, and rarely return parameter values or call themselves recursively."))
    aux2 = insFld(aux1, gFld("<strong>get-start-*</strong>"))
      insDoc(aux2, gFld("These methods often read from fields and return field values. They never write parameter values to fields, and very rarely return void, and rarely write to fields, use local variables, throw exceptions or call themselves recursively."))
    aux2 = insFld(aux1, gFld("<strong>get-system-*</strong>"))
      insDoc(aux2, gFld("These methods comparatively often are static. They never return void or call themselves recursively, and very rarely write parameter values to fields, and rarely write to fields or throw exceptions."))
    aux2 = insFld(aux1, gFld("<strong>get-target-*</strong>"))
      insDoc(aux2, gFld("These methods often return field values. They never call themselves recursively, and very rarely return void or write parameter values to fields, and rarely call other methods with the same name or return parameter values."))
    aux2 = insFld(aux1, gFld("<strong>get-text-*</strong>"))
      insDoc(aux2, gFld("These methods often have multiple return points. They very rarely write parameter values to fields, and rarely return void, write to fields or call themselves recursively."))
     aux3 = insFld(aux2, gFld("<strong>get-text</strong>"))
       insDoc(aux3, gFld("These methods often do runtime type-checking or casting and have multiple return points. They very rarely write parameter values to fields, and rarely return void, write to fields, return parameter values or call themselves recursively."))
    aux2 = insFld(aux1, gFld("<strong>get-time-*</strong>"))
      insDoc(aux2, gFld("These methods often return field values. They very rarely write parameter values to fields, and rarely return void, contain loops, do runtime type-checking or casting, return parameter values or call themselves recursively."))
    aux2 = insFld(aux1, gFld("<strong>get-type-*</strong>"))
      insDoc(aux2, gFld("These methods very rarely return void or write parameter values to fields, and rarely write to fields or return parameter values."))
     aux3 = insFld(aux2, gFld("<strong>get-type</strong>"))
       insDoc(aux3, gFld("These methods often return field values. They never return void, and very rarely write parameter values to fields, return parameter values or call themselves recursively, and rarely write to fields, contain loops, use local variables or create objects."))
    aux2 = insFld(aux1, gFld("<strong>get-user-*</strong>"))
      insDoc(aux2, gFld("These methods often return field values, and comparatively often are static. They never call themselves recursively, and very rarely write parameter values to fields, and rarely return void."))
    aux2 = insFld(aux1, gFld("<strong>get-value-*</strong>"))
      insDoc(aux2, gFld("These methods often do runtime type-checking or casting and have multiple return points. They very rarely return void or write parameter values to fields, and rarely write to fields or call themselves recursively. The phrase appears in most applications."))
     aux3 = insFld(aux2, gFld("<strong>get-value</strong>"))
       insDoc(aux3, gFld("These methods often have multiple return points. They very rarely return void or write parameter values to fields, and rarely write to fields, are static or call themselves recursively. The phrase appears in most applications."))
    aux2 = insFld(aux1, gFld("<strong>get-version-*</strong>"))
      insDoc(aux2, gFld("These methods often return field values, and comparatively often are static. They never return parameter values, and very rarely return void or write parameter values to fields, and rarely have parameters, write to fields or call themselves recursively."))
     aux3 = insFld(aux2, gFld("<strong>get-version</strong>"))
       insDoc(aux3, gFld("These methods often return field values, and comparatively often are static. They never return parameter values, and very rarely return void or write parameter values to fields, and rarely have parameters, write to fields, contain loops, use local variables or have branches."))
    aux2 = insFld(aux1, gFld("<strong>get-[/noun]-*</strong>"))
      insDoc(aux2, gFld("These methods very rarely write parameter values to fields, and rarely return void, return parameter values or call themselves recursively. The phrase appears in all applications."))
    aux2 = insFld(aux1, gFld("<strong>get-base-*</strong>"))
      insDoc(aux2, gFld("These methods never write parameter values to fields, and very rarely return void, and rarely write to fields, call other methods with the same name or call themselves recursively."))
      aux3 = insFld(aux2, gFld("<strong>get-base-[noun]-*</strong>"))
        insDoc(aux3, gFld("These methods often return field values. They never write parameter values to fields, and very rarely return void, and rarely write to fields."))
    aux2 = insFld(aux1, gFld("<strong>get-current-*</strong>"))
      insDoc(aux2, gFld("These methods often read from fields, and comparatively often are static. They very rarely write parameter values to fields, return parameter values or call themselves recursively, and rarely have parameters or return void."))
      aux3 = insFld(aux2, gFld("<strong>get-current-[noun]-*</strong>"))
        insDoc(aux3, gFld("These methods often read from fields, and comparatively often are static. They never write parameter values to fields or call themselves recursively, and rarely have parameters, return void or return parameter values."))
       aux4 = insFld(aux3, gFld("<strong>get-current-[noun]</strong>"))
         insDoc(aux4, gFld("These methods often read from fields and return field values, and comparatively often are static. They never write parameter values to fields or call themselves recursively, and very rarely return void or return parameter values, and rarely have parameters."))
    aux2 = insFld(aux1, gFld("<strong>get-default-*</strong>"))
      insDoc(aux2, gFld("These methods often are static. They very rarely return void or write parameter values to fields, and rarely call other methods with the same name, return parameter values or call themselves recursively."))
      aux3 = insFld(aux2, gFld("<strong>get-default-[noun]-*</strong>"))
        insDoc(aux3, gFld("These methods often are static. They very rarely return void or write parameter values to fields, and rarely call other methods with the same name, return parameter values or call themselves recursively."))
       aux4 = insFld(aux3, gFld("<strong>get-default-[noun]</strong>"))
         insDoc(aux4, gFld("These methods comparatively often are static. They very rarely write parameter values to fields, and rarely return void."))
    aux2 = insFld(aux1, gFld("<strong>get-first-*</strong>"))
      insDoc(aux2, gFld("These methods often contain loops and have multiple return points, and comparatively often are static and call themselves recursively. They never write parameter values to fields, and rarely return void or write to fields."))
      aux3 = insFld(aux2, gFld("<strong>get-first-[noun]-*</strong>"))
        insDoc(aux3, gFld("These methods often contain loops and have multiple return points, and comparatively often are static. They never write parameter values to fields, and very rarely return void, and rarely create objects."))
    aux2 = insFld(aux1, gFld("<strong>get-last-*</strong>"))
      insDoc(aux2, gFld("These methods never return void, and very rarely write parameter values to fields."))
      aux3 = insFld(aux2, gFld("<strong>get-last-[noun]-*</strong>"))
        insDoc(aux3, gFld("These methods comparatively often are static. They never return void or write parameter values to fields, and rarely write to fields or create objects."))
    aux2 = insFld(aux1, gFld("<strong>get-local-*</strong>"))
      insDoc(aux2, gFld("These methods very rarely return void or write parameter values to fields, and rarely write to fields or call themselves recursively."))
      aux3 = insFld(aux2, gFld("<strong>get-local-[noun]-*</strong>"))
        insDoc(aux3, gFld("These methods never return void, and very rarely write parameter values to fields, and rarely write to fields or call themselves recursively."))
       aux4 = insFld(aux3, gFld("<strong>get-local-[noun]</strong>"))
         insDoc(aux4, gFld("These methods never return void, and very rarely write parameter values to fields, and rarely write to fields or call themselves recursively."))
    aux2 = insFld(aux1, gFld("<strong>get-next-*</strong>"))
      insDoc(aux2, gFld("These methods often write to fields, contain loops, use local variables, have branches and have multiple return points. They very rarely write parameter values to fields, and rarely return void."))
      aux3 = insFld(aux2, gFld("<strong>get-next-[noun]-*</strong>"))
        insDoc(aux3, gFld("These methods often write to fields, contain loops, use local variables, have branches and have multiple return points. They very rarely write parameter values to fields, and rarely return void."))
       aux4 = insFld(aux3, gFld("<strong>get-next-[noun]</strong>"))
         insDoc(aux4, gFld("These methods often have branches and have multiple return points, and comparatively often contain loops. They very rarely write parameter values to fields, and rarely return void."))
    aux2 = insFld(aux1, gFld("<strong>get-[/adjective]-*</strong>"))
      insDoc(aux2, gFld("These methods comparatively often are static. They very rarely return void or write parameter values to fields. The phrase appears in practically all applications."))
    aux2 = insFld(aux1, gFld("<strong>get-[adverb]-*</strong>"))
      insDoc(aux2, gFld("These methods often contain loops, and comparatively often are static. They never write parameter values to fields, and rarely return void."))
    aux2 = insFld(aux1, gFld("<strong>get-[preposition]-*</strong>"))
      insDoc(aux2, gFld("These methods often do runtime type-checking or casting and have multiple return points, and comparatively often are static. They very rarely return void or write parameter values to fields, and rarely write to fields or return parameter values."))
      aux3 = insFld(aux2, gFld("<strong>get-[preposition]-[noun]-*</strong>"))
        insDoc(aux3, gFld("These methods often do runtime type-checking or casting and have multiple return points. They never write parameter values to fields, and very rarely return void, and rarely write to fields, call other methods with the same name, return parameter values or call themselves recursively."))
       aux4 = insFld(aux3, gFld("<strong>get-[preposition]-[noun]</strong>"))
         insDoc(aux4, gFld("These methods often do runtime type-checking or casting and have multiple return points. They never write parameter values to fields, and very rarely return void, and rarely write to fields, call other methods with the same name, return parameter values or call themselves recursively."))
    aux2 = insFld(aux1, gFld("<strong>get-all-*</strong>"))
      insDoc(aux2, gFld("These methods very often contain loops, and often use local variables, create objects, do runtime type-checking or casting, are static and have branches, and comparatively often call themselves recursively. They never write parameter values to fields, and rarely return void or return parameter values."))
    aux2 = insFld(aux1, gFld("<strong>get-Object-*</strong>"))
      insDoc(aux2, gFld("These methods often throw exceptions, do runtime type-checking or casting and have multiple return points. They never return void, write parameter values to fields or call themselves recursively."))
    aux2 = insFld(aux1, gFld("<strong>get-String-*</strong>"))
      insDoc(aux2, gFld("These methods often call other methods with the same name, are static and have multiple return points, and comparatively often return parameter values. They very rarely return void or write parameter values to fields, and rarely write to fields."))
     aux3 = insFld(aux2, gFld("<strong>get-String</strong>"))
       insDoc(aux3, gFld("These methods often call other methods with the same name, are static and have multiple return points, and comparatively often return parameter values. They very rarely return void or write parameter values to fields, and rarely write to fields or call themselves recursively."))
    aux2 = insFld(aux1, gFld("<strong>get-[/type]-*</strong>"))
      insDoc(aux2, gFld("These methods very rarely return void or write parameter values to fields, and rarely return parameter values or call themselves recursively. The phrase appears in practically all applications."))
  aux1 = insFld(foldersTree, gFld("<strong>handle-*</strong>"))
    insDoc(aux1, gFld("These methods often use local variables, do runtime type-checking or casting, have branches and have multiple return points, and comparatively often contain loops. They very rarely return field values, and rarely call other methods with the same name, return parameter values or call themselves recursively."))
    aux2 = insFld(aux1, gFld("<strong>handle-[noun]-*</strong>"))
      insDoc(aux2, gFld("These methods often use local variables, have branches and have multiple return points. They very rarely return field values, and rarely call other methods with the same name or call themselves recursively."))
     aux3 = insFld(aux2, gFld("<strong>handle-[noun]</strong>"))
       insDoc(aux3, gFld("These methods often have parameters, use local variables, have branches and have multiple return points. They very rarely return field values, and rarely call other methods with the same name, return parameter values or call themselves recursively."))
  aux1 = insFld(foldersTree, gFld("<strong>has-*</strong>"))
    insDoc(aux1, gFld("These methods often have branches and have multiple return points. They very rarely write parameter values to fields or return parameter values, and rarely return void, create objects or throw exceptions. The phrase appears in most applications."))
    aux2 = insFld(aux1, gFld("<strong>has-[noun]-*</strong>"))
      insDoc(aux2, gFld("These methods often have branches and have multiple return points. They never return parameter values, and very rarely write to fields or write parameter values to fields, and rarely return void, create objects or throw exceptions."))
     aux3 = insFld(aux2, gFld("<strong>has-[noun]</strong>"))
       insDoc(aux3, gFld("These methods often have branches and have multiple return points. They never write parameter values to fields or return parameter values, and very rarely write to fields, and rarely return void, create objects or call themselves recursively."))
      aux3 = insFld(aux2, gFld("<strong>has-[noun]-[noun]-*</strong>"))
        insDoc(aux3, gFld("These methods often have branches and have multiple return points. They never return parameter values, and very rarely call themselves recursively, and rarely return void, write to fields, create objects, throw exceptions or write parameter values to fields."))
       aux4 = insFld(aux3, gFld("<strong>has-[noun]-[noun]</strong>"))
         insDoc(aux4, gFld("These methods often have branches and have multiple return points. They never return parameter values, and very rarely call themselves recursively, and rarely return void, write to fields, create objects, throw exceptions or write parameter values to fields."))
    aux2 = insFld(aux1, gFld("<strong>has-next-*</strong>"))
      insDoc(aux2, gFld("These methods very often read from fields, and often call other methods with the same name and have branches. They never return void, write parameter values to fields or return parameter values, and very rarely have parameters or are static, and rarely use local variables or create objects."))
     aux3 = insFld(aux2, gFld("<strong>has-next</strong>"))
       insDoc(aux3, gFld("These methods very often read from fields, and often call other methods with the same name and have branches. They never return void, write parameter values to fields or return parameter values, and very rarely have parameters or are static, and rarely use local variables or create objects."))
    aux2 = insFld(aux1, gFld("<strong>has-[/adjective]-*</strong>"))
      insDoc(aux2, gFld("These methods often have branches and have multiple return points, and comparatively often are static. They never return parameter values, and very rarely return void or write parameter values to fields, and rarely write to fields, create objects or throw exceptions."))
    aux2 = insFld(aux1, gFld("<strong>has-more-*</strong>"))
      insDoc(aux2, gFld("These methods often have branches and have multiple return points. They never return void, write parameter values to fields or return parameter values, and very rarely are static, and rarely have parameters, use local variables, create objects, do runtime type-checking or casting, return field values or call themselves recursively."))
  aux1 = insFld(foldersTree, gFld("<strong>hash-*</strong>"))
    insDoc(aux1, gFld("These methods very often call other methods with the same name. They never write parameter values to fields or call themselves recursively, and very rarely return void or do runtime type-checking or casting, and rarely have parameters, create objects, throw exceptions or return parameter values. The phrase appears in most applications."))
    aux2 = insFld(aux1, gFld("<strong>hash-code-*</strong>"))
      insDoc(aux2, gFld("These methods very often call other methods with the same name, and often read from fields. They never return void, write parameter values to fields or call themselves recursively, and very rarely do runtime type-checking or casting or return parameter values, and rarely have parameters, create objects, throw exceptions or are static. The phrase appears in most applications."))
     aux3 = insFld(aux2, gFld("<strong>hash-code</strong>"))
       insDoc(aux3, gFld("These methods very often call other methods with the same name, and often read from fields. They never return void, write parameter values to fields or call themselves recursively, and very rarely do runtime type-checking or casting or return parameter values, and rarely have parameters, create objects, throw exceptions or are static. The phrase appears in most applications."))
  aux1 = insFld(foldersTree, gFld("<strong>init-*</strong>"))
    insDoc(aux1, gFld("These methods often write to fields, use local variables and create objects. They rarely return field values, return parameter values or call themselves recursively. The phrase appears in most applications."))
   aux2 = insFld(aux1, gFld("<strong>init</strong>"))
     insDoc(aux2, gFld("These methods often return void, write to fields and create objects. They very rarely return field values or call themselves recursively, and rarely return parameter values or have multiple return points."))
    aux2 = insFld(aux1, gFld("<strong>init-[noun]-*</strong>"))
      insDoc(aux2, gFld("These methods often write to fields, contain loops, use local variables, create objects and have branches. They rarely return field values, return parameter values or call themselves recursively."))
     aux3 = insFld(aux2, gFld("<strong>init-[noun]</strong>"))
       insDoc(aux3, gFld("These methods often write to fields, contain loops, use local variables and create objects. They very rarely call themselves recursively, and rarely return field values or return parameter values."))
  aux1 = insFld(foldersTree, gFld("<strong>initialize-*</strong>"))
    insDoc(aux1, gFld("These methods often write to fields and contain loops. They very rarely return field values or return parameter values, and rarely call themselves recursively."))
   aux2 = insFld(aux1, gFld("<strong>initialize</strong>"))
     insDoc(aux2, gFld("These methods often write to fields. They never return parameter values, and very rarely return field values or call themselves recursively."))
  aux1 = insFld(foldersTree, gFld("<strong>insert-*</strong>"))
    insDoc(aux1, gFld("These methods often have parameters, use local variables, throw exceptions and are static. They very rarely return field values."))
    aux2 = insFld(aux1, gFld("<strong>insert-[noun]-*</strong>"))
      insDoc(aux2, gFld("These methods often have parameters, and comparatively often contain loops. They very rarely return field values."))
     aux3 = insFld(aux2, gFld("<strong>insert-[noun]</strong>"))
       insDoc(aux3, gFld("These methods often have parameters. They very rarely return field values."))
  aux1 = insFld(foldersTree, gFld("<strong>invoke-*</strong>"))
    insDoc(aux1, gFld("These methods often have parameters, call other methods with the same name, use local variables, create objects, throw exceptions, do runtime type-checking or casting, have branches and have multiple return points. They rarely write parameter values to fields or return parameter values."))
   aux2 = insFld(aux1, gFld("<strong>invoke</strong>"))
     insDoc(aux2, gFld("These methods very often throw exceptions, and often have parameters, call other methods with the same name, use local variables, create objects, do runtime type-checking or casting, have branches and have multiple return points. They rarely return parameter values."))
  aux1 = insFld(foldersTree, gFld("<strong>is-*</strong>"))
    insDoc(aux1, gFld("These methods often have multiple return points, and comparatively often are static. They very rarely return void, write to fields, write parameter values to fields or return parameter values, and rarely create objects. The phrase appears in practically all applications."))
    aux2 = insFld(aux1, gFld("<strong>is-[verb]-*</strong>"))
      insDoc(aux2, gFld("These methods comparatively often are static. They never write parameter values to fields, and very rarely return void, write to fields, return parameter values or call themselves recursively, and rarely call other methods with the same name, contain loops, use local variables, create objects or throw exceptions."))
      aux3 = insFld(aux2, gFld("<strong>is-[verb]-[noun]-*</strong>"))
        insDoc(aux3, gFld("These methods often return field values, and comparatively often are static. They never return void, write parameter values to fields or return parameter values, and very rarely write to fields, and rarely call other methods with the same name, contain loops, create objects, throw exceptions or call themselves recursively."))
    aux2 = insFld(aux1, gFld("<strong>is-[noun]-*</strong>"))
      insDoc(aux2, gFld("These methods often are static and have multiple return points. They very rarely return void, write to fields, write parameter values to fields or return parameter values, and rarely create objects or throw exceptions. The phrase appears in most applications."))
     aux3 = insFld(aux2, gFld("<strong>is-[noun]</strong>"))
       insDoc(aux3, gFld("These methods often are static and have multiple return points. They very rarely write to fields, write parameter values to fields or return parameter values, and rarely return void, create objects or throw exceptions. The phrase appears in most applications."))
      aux3 = insFld(aux2, gFld("<strong>is-[noun]-[noun]-*</strong>"))
        insDoc(aux3, gFld("These methods often are static and have multiple return points. They very rarely return void, write to fields, write parameter values to fields or return parameter values, and rarely call other methods with the same name, create objects, throw exceptions or call themselves recursively. The phrase appears in most applications."))
       aux4 = insFld(aux3, gFld("<strong>is-[noun]-[noun]</strong>"))
         insDoc(aux4, gFld("These methods often are static and have multiple return points. They very rarely return void, write to fields, write parameter values to fields or return parameter values, and rarely call other methods with the same name, create objects, throw exceptions or call themselves recursively."))
        aux4 = insFld(aux3, gFld("<strong>is-[noun]-[noun]-[noun]-*</strong>"))
          insDoc(aux4, gFld("These methods often are static and have multiple return points. They never write parameter values to fields or return parameter values, and very rarely return void, write to fields or throw exceptions, and rarely call other methods with the same name, create objects or call themselves recursively."))
         aux5 = insFld(aux4, gFld("<strong>is-[noun]-[noun]-[noun]</strong>"))
           insDoc(aux5, gFld("These methods often are static, have branches and have multiple return points. They never write parameter values to fields or return parameter values, and very rarely return void, write to fields or throw exceptions, and rarely call other methods with the same name, create objects or call themselves recursively."))
      aux3 = insFld(aux2, gFld("<strong>is-[noun]-[adjective]-*</strong>"))
        insDoc(aux3, gFld("These methods often have multiple return points. They never return parameter values, and very rarely return void, write to fields or write parameter values to fields, and rarely create objects or call themselves recursively."))
       aux4 = insFld(aux3, gFld("<strong>is-[noun]-[adjective]</strong>"))
         insDoc(aux4, gFld("These methods often have multiple return points. They never return parameter values, and very rarely return void or write parameter values to fields, and rarely write to fields, create objects or call themselves recursively."))
    aux2 = insFld(aux1, gFld("<strong>is-empty-*</strong>"))
      insDoc(aux2, gFld("These methods often call other methods with the same name and have branches. They never return parameter values or call themselves recursively, and very rarely return void, write to fields or write parameter values to fields, and rarely have parameters, use local variables, create objects, do runtime type-checking or casting or return field values."))
     aux3 = insFld(aux2, gFld("<strong>is-empty</strong>"))
       insDoc(aux3, gFld("These methods often call other methods with the same name and have branches. They never return parameter values or call themselves recursively, and very rarely return void or write to fields, and rarely have parameters, contain loops, use local variables, create objects, do runtime type-checking or casting, write parameter values to fields or return field values."))
    aux2 = insFld(aux1, gFld("<strong>is-valid-*</strong>"))
      insDoc(aux2, gFld("These methods very often have multiple return points, and often contain loops, are static and have branches. They never write parameter values to fields or return parameter values, and rarely return void, write to fields or throw exceptions."))
    aux2 = insFld(aux1, gFld("<strong>is-[/adjective]-*</strong>"))
      insDoc(aux2, gFld("These methods often have multiple return points, and comparatively often are static. They very rarely return void, write to fields, write parameter values to fields or return parameter values, and rarely create objects. The phrase appears in practically all applications."))
    aux2 = insFld(aux1, gFld("<strong>is-[preposition]-*</strong>"))
      insDoc(aux2, gFld("These methods often have branches and have multiple return points, and comparatively often are static. They never write parameter values to fields, and very rarely return void or return parameter values, and rarely write to fields or create objects."))
  aux1 = insFld(foldersTree, gFld("<strong>load-*</strong>"))
    insDoc(aux1, gFld("These methods very often use local variables, and often contain loops, create objects, throw exceptions, do runtime type-checking or casting, are static, have branches and have multiple return points. They rarely return parameter values."))
   aux2 = insFld(aux1, gFld("<strong>load</strong>"))
     insDoc(aux2, gFld("These methods often contain loops, use local variables, create objects, throw exceptions, are static, have branches and have multiple return points. They rarely return field values or return parameter values."))
    aux2 = insFld(aux1, gFld("<strong>load-[noun]-*</strong>"))
      insDoc(aux2, gFld("These methods very often use local variables, and often contain loops, create objects, throw exceptions, do runtime type-checking or casting, are static, have branches and have multiple return points. They rarely write parameter values to fields or return parameter values."))
     aux3 = insFld(aux2, gFld("<strong>load-[noun]</strong>"))
       insDoc(aux3, gFld("These methods often contain loops, use local variables, create objects, throw exceptions, do runtime type-checking or casting, are static, have branches and have multiple return points. They rarely return parameter values."))
    aux2 = insFld(aux1, gFld("<strong>load-Class-*</strong>"))
      insDoc(aux2, gFld("These methods often have parameters, call other methods with the same name, use local variables, create objects, throw exceptions, are static, have branches and have multiple return points, and comparatively often call themselves recursively. They never return parameter values, and rarely return void, write to fields, write parameter values to fields or return field values."))
     aux3 = insFld(aux2, gFld("<strong>load-Class</strong>"))
       insDoc(aux3, gFld("These methods very often call other methods with the same name, and often have parameters, use local variables, create objects, throw exceptions, are static, have branches and have multiple return points, and comparatively often call themselves recursively. They never return parameter values, and rarely return void, write to fields, write parameter values to fields or return field values."))
  aux1 = insFld(foldersTree, gFld("<strong>make-*</strong>"))
    insDoc(aux1, gFld("These methods very often create objects, and often are static."))
    aux2 = insFld(aux1, gFld("<strong>make-[noun]-*</strong>"))
      insDoc(aux2, gFld("These methods very often create objects, and often use local variables, do runtime type-checking or casting and are static. They rarely call other methods with the same name."))
     aux3 = insFld(aux2, gFld("<strong>make-[noun]</strong>"))
       insDoc(aux3, gFld("These methods very often create objects, and often do runtime type-checking or casting and are static. They rarely call other methods with the same name."))
  aux1 = insFld(foldersTree, gFld("<strong>open-*</strong>"))
    insDoc(aux1, gFld("These methods often write to fields, use local variables, create objects, throw exceptions, are static, have branches and have multiple return points. They rarely return parameter values or call themselves recursively."))
    aux2 = insFld(aux1, gFld("<strong>open-[noun]-*</strong>"))
      insDoc(aux2, gFld("These methods often use local variables, create objects, are static, have branches and have multiple return points. They very rarely return parameter values, and rarely return field values or call themselves recursively."))
  aux1 = insFld(foldersTree, gFld("<strong>parse-*</strong>"))
    insDoc(aux1, gFld("These methods very often call other methods with the same name, do runtime type-checking or casting and are static, and often have parameters and read from fields. They rarely return field values. The phrase appears in most applications."))
   aux2 = insFld(aux1, gFld("<strong>parse</strong>"))
     insDoc(aux2, gFld("These methods very often read from fields, call other methods with the same name, do runtime type-checking or casting and are static, and often have parameters. They very rarely return parameter values or call themselves recursively, and rarely use local variables, write parameter values to fields, return field values, have branches or have multiple return points."))
    aux2 = insFld(aux1, gFld("<strong>parse-[noun]-*</strong>"))
      insDoc(aux2, gFld("These methods very often contain loops, use local variables and have branches, and often write to fields, create objects, throw exceptions, are static and have multiple return points. They rarely call other methods with the same name or return field values."))
     aux3 = insFld(aux2, gFld("<strong>parse-[noun]</strong>"))
       insDoc(aux3, gFld("These methods very often contain loops and use local variables, and often write to fields, create objects, throw exceptions, are static, have branches and have multiple return points. They rarely call other methods with the same name."))
      aux3 = insFld(aux2, gFld("<strong>parse-[noun]-[noun]-*</strong>"))
        insDoc(aux3, gFld("These methods very often contain loops, use local variables and have branches, and often write to fields, create objects, throw exceptions, are static and have multiple return points. They rarely call other methods with the same name or return field values."))
       aux4 = insFld(aux3, gFld("<strong>parse-[noun]-[noun]</strong>"))
         insDoc(aux4, gFld("These methods very often contain loops, use local variables and have branches, and often write to fields, create objects, throw exceptions, are static and have multiple return points. They rarely call other methods with the same name or return field values."))
    aux2 = insFld(aux1, gFld("<strong>parse-[type]-*</strong>"))
      insDoc(aux2, gFld("These methods very often contain loops, use local variables and create objects, and often have parameters, throw exceptions, are static, have branches and have multiple return points. They rarely return field values."))
     aux3 = insFld(aux2, gFld("<strong>parse-[type]</strong>"))
       insDoc(aux3, gFld("These methods very often contain loops, use local variables and create objects, and often have parameters, throw exceptions, are static, have branches and have multiple return points. They rarely return field values."))
  aux1 = insFld(foldersTree, gFld("<strong>print-*</strong>"))
    insDoc(aux1, gFld("These methods often contain loops and have branches, and comparatively often are static. They very rarely return field values, and rarely write parameter values to fields."))
   aux2 = insFld(aux1, gFld("<strong>print</strong>"))
     insDoc(aux2, gFld("These methods often have parameters, call other methods with the same name and contain loops. They very rarely return field values, and rarely write parameter values to fields."))
    aux2 = insFld(aux1, gFld("<strong>print-stack-*</strong>"))
      insDoc(aux2, gFld("These methods very often call other methods with the same name, and often return void and have branches. They never write parameter values to fields, return field values or return parameter values, and very rarely write to fields or have multiple return points, and rarely do runtime type-checking or casting or call themselves recursively."))
      aux3 = insFld(aux2, gFld("<strong>print-stack-trace-*</strong>"))
        insDoc(aux3, gFld("These methods very often call other methods with the same name, and often return void and have branches. They never write parameter values to fields, return field values or return parameter values, and very rarely write to fields or have multiple return points, and rarely do runtime type-checking or casting or call themselves recursively."))
       aux4 = insFld(aux3, gFld("<strong>print-stack-trace</strong>"))
         insDoc(aux4, gFld("These methods very often call other methods with the same name, and often return void and have branches. They never write parameter values to fields, return field values or return parameter values, and very rarely write to fields or have multiple return points, and rarely do runtime type-checking or casting or call themselves recursively."))
    aux2 = insFld(aux1, gFld("<strong>print-[/noun]-*</strong>"))
      insDoc(aux2, gFld("These methods often contain loops, use local variables, create objects, are static and have branches. They very rarely return field values, and rarely write parameter values to fields."))
  aux1 = insFld(foldersTree, gFld("<strong>process-*</strong>"))
    insDoc(aux1, gFld("These methods very often contain loops, use local variables and have branches, and often create objects, throw exceptions, do runtime type-checking or casting and have multiple return points, and comparatively often call themselves recursively. They rarely return field values."))
    aux2 = insFld(aux1, gFld("<strong>process-[noun]-*</strong>"))
      insDoc(aux2, gFld("These methods very often contain loops, use local variables and have branches, and often create objects, throw exceptions, do runtime type-checking or casting and have multiple return points, and comparatively often call themselves recursively. They rarely call other methods with the same name, write parameter values to fields or return field values."))
     aux3 = insFld(aux2, gFld("<strong>process-[noun]</strong>"))
       insDoc(aux3, gFld("These methods very often contain loops and have branches, and often use local variables, create objects, throw exceptions, do runtime type-checking or casting and have multiple return points, and comparatively often call themselves recursively. They rarely return field values."))
      aux3 = insFld(aux2, gFld("<strong>process-[noun]-[noun]-*</strong>"))
        insDoc(aux3, gFld("These methods very often contain loops, use local variables and have branches, and often create objects, throw exceptions, do runtime type-checking or casting and have multiple return points. They rarely call other methods with the same name, write parameter values to fields or return field values."))
       aux4 = insFld(aux3, gFld("<strong>process-[noun]-[noun]</strong>"))
         insDoc(aux4, gFld("These methods very often contain loops, use local variables and have branches, and often create objects, throw exceptions, do runtime type-checking or casting and have multiple return points, and comparatively often call themselves recursively. They rarely call other methods with the same name, write parameter values to fields or return field values."))
    aux2 = insFld(aux1, gFld("<strong>process-[type]-*</strong>"))
      insDoc(aux2, gFld("These methods very often contain loops, use local variables and have branches, and often have parameters, create objects, throw exceptions, do runtime type-checking or casting and have multiple return points, and comparatively often call themselves recursively. They rarely return field values."))
  aux1 = insFld(foldersTree, gFld("<strong>put-*</strong>"))
    insDoc(aux1, gFld("These methods often have parameters, contain loops and throw exceptions. They rarely return field values."))
   aux2 = insFld(aux1, gFld("<strong>put</strong>"))
     insDoc(aux2, gFld("These methods very often have parameters, and often call other methods with the same name, use local variables, create objects and throw exceptions. They rarely are static."))
  aux1 = insFld(foldersTree, gFld("<strong>read-*</strong>"))
    insDoc(aux1, gFld("These methods often write to fields, contain loops, use local variables, create objects, throw exceptions, are static and have branches. They rarely return field values. The phrase appears in most applications."))
   aux2 = insFld(aux1, gFld("<strong>read</strong>"))
     insDoc(aux2, gFld("These methods very often are static, and often write to fields, call other methods with the same name, contain loops, use local variables, create objects, throw exceptions, have branches and have multiple return points. They rarely write parameter values to fields or return field values."))
    aux2 = insFld(aux1, gFld("<strong>read-[noun]-*</strong>"))
      insDoc(aux2, gFld("These methods often write to fields, contain loops, use local variables, create objects, throw exceptions and have branches, and comparatively often are static. The phrase appears in most applications."))
     aux3 = insFld(aux2, gFld("<strong>read-[noun]</strong>"))
       insDoc(aux3, gFld("These methods often write to fields, contain loops, use local variables, create objects, throw exceptions and have branches. They rarely write parameter values to fields. The phrase appears in most applications."))
      aux3 = insFld(aux2, gFld("<strong>read-[noun]-[noun]-*</strong>"))
        insDoc(aux3, gFld("These methods very often contain loops, and often write to fields, use local variables, create objects, throw exceptions, are static, have branches and have multiple return points. They rarely call other methods with the same name or return field values."))
       aux4 = insFld(aux3, gFld("<strong>read-[noun]-[noun]</strong>"))
         insDoc(aux4, gFld("These methods very often contain loops, and often write to fields, use local variables, create objects, throw exceptions, are static, have branches and have multiple return points. They rarely call other methods with the same name or return field values."))
    aux2 = insFld(aux1, gFld("<strong>read-[adjective]-*</strong>"))
      insDoc(aux2, gFld("These methods often contain loops, use local variables, create objects, throw exceptions, are static and have branches. They rarely write parameter values to fields, return field values or call themselves recursively."))
    aux2 = insFld(aux1, gFld("<strong>read-[type]-*</strong>"))
      insDoc(aux2, gFld("These methods very often use local variables, and often write to fields, contain loops, create objects, throw exceptions, do runtime type-checking or casting and have branches, and comparatively often are static. They very rarely return parameter values, and rarely return field values."))
     aux3 = insFld(aux2, gFld("<strong>read-[type]</strong>"))
       insDoc(aux3, gFld("These methods often write to fields, call other methods with the same name, contain loops, use local variables, create objects, throw exceptions, do runtime type-checking or casting and have branches, and comparatively often are static. They very rarely return parameter values, and rarely write parameter values to fields or return field values."))
  aux1 = insFld(foldersTree, gFld("<strong>register-*</strong>"))
    insDoc(aux1, gFld("These methods often use local variables, create objects, throw exceptions, are static and have branches. They very rarely return field values."))
   aux2 = insFld(aux1, gFld("<strong>register</strong>"))
     insDoc(aux2, gFld("These methods often read from fields, use local variables, create objects, throw exceptions, are static and have branches. They never return parameter values, and very rarely return field values."))
    aux2 = insFld(aux1, gFld("<strong>register-[noun]-*</strong>"))
      insDoc(aux2, gFld("These methods often use local variables, create objects, are static and have branches. They very rarely return field values."))
     aux3 = insFld(aux2, gFld("<strong>register-[noun]</strong>"))
       insDoc(aux3, gFld("These methods often use local variables, create objects, are static and have branches. They very rarely return field values."))
  aux1 = insFld(foldersTree, gFld("<strong>remove-*</strong>"))
    insDoc(aux1, gFld("These methods comparatively often are static. They very rarely write parameter values to fields or return field values. The phrase appears in most applications."))
   aux2 = insFld(aux1, gFld("<strong>remove</strong>"))
     insDoc(aux2, gFld("These methods often call other methods with the same name and throw exceptions. They rarely write parameter values to fields, return field values or call themselves recursively."))
    aux2 = insFld(aux1, gFld("<strong>remove-[noun]-*</strong>"))
      insDoc(aux2, gFld("These methods often have parameters. They very rarely write parameter values to fields or return field values. The phrase appears in most applications."))
     aux3 = insFld(aux2, gFld("<strong>remove-[noun]</strong>"))
       insDoc(aux3, gFld("These methods often have parameters. They very rarely write parameter values to fields or return field values. The phrase appears in most applications."))
      aux3 = insFld(aux2, gFld("<strong>remove-[noun]-[noun]-*</strong>"))
        insDoc(aux3, gFld("These methods often have parameters. They very rarely write parameter values to fields or return field values, and rarely call other methods with the same name, return parameter values or call themselves recursively."))
       aux4 = insFld(aux3, gFld("<strong>remove-[noun]-[noun]</strong>"))
         insDoc(aux4, gFld("These methods often have parameters. They very rarely write parameter values to fields or return field values, and rarely call other methods with the same name, return parameter values or call themselves recursively."))
    aux2 = insFld(aux1, gFld("<strong>remove-[adjective]-*</strong>"))
      insDoc(aux2, gFld("These methods often contain loops, use local variables and have branches. They never write parameter values to fields, and rarely return field values."))
      aux3 = insFld(aux2, gFld("<strong>remove-[adjective]-[noun]-*</strong>"))
        insDoc(aux3, gFld("These methods often contain loops and have branches. They never write parameter values to fields or return field values."))
       aux4 = insFld(aux3, gFld("<strong>remove-[adjective]-[noun]</strong>"))
         insDoc(aux4, gFld("These methods often contain loops, use local variables and have branches. They never write parameter values to fields or return field values."))
    aux2 = insFld(aux1, gFld("<strong>remove-all-*</strong>"))
      insDoc(aux2, gFld("These methods often contain loops and throw exceptions. They never write parameter values to fields or return field values, and rarely return parameter values."))
    aux2 = insFld(aux1, gFld("<strong>remove-[type]-*</strong>"))
      insDoc(aux2, gFld("These methods often have parameters, read from fields and call other methods with the same name. They very rarely write parameter values to fields or return field values."))
     aux3 = insFld(aux2, gFld("<strong>remove-[type]</strong>"))
       insDoc(aux3, gFld("These methods very often call other methods with the same name, and often have parameters and read from fields. They very rarely write parameter values to fields or return field values, and rarely return parameter values or call themselves recursively."))
  aux1 = insFld(foldersTree, gFld("<strong>reset-*</strong>"))
    insDoc(aux1, gFld("These methods often return void and write to fields. They very rarely return field values or return parameter values, and rarely call themselves recursively or have multiple return points."))
   aux2 = insFld(aux1, gFld("<strong>reset</strong>"))
     insDoc(aux2, gFld("These methods often write to fields. They never return parameter values or call themselves recursively, and very rarely return field values, and rarely are static or have multiple return points."))
    aux2 = insFld(aux1, gFld("<strong>reset-[noun]-*</strong>"))
      insDoc(aux2, gFld("These methods often return void and write to fields, and comparatively often contain loops. They very rarely return parameter values, and rarely return field values or call themselves recursively."))
     aux3 = insFld(aux2, gFld("<strong>reset-[noun]</strong>"))
       insDoc(aux3, gFld("These methods often return void and write to fields. They never return parameter values, and rarely return field values, call themselves recursively or have multiple return points."))
  aux1 = insFld(foldersTree, gFld("<strong>resolve-*</strong>"))
    insDoc(aux1, gFld("These methods often contain loops, use local variables, create objects, do runtime type-checking or casting, have branches and have multiple return points, and comparatively often are static, return parameter values and call themselves recursively."))
    aux2 = insFld(aux1, gFld("<strong>resolve-[noun]-*</strong>"))
      insDoc(aux2, gFld("These methods often contain loops, use local variables, create objects, do runtime type-checking or casting, have branches and have multiple return points, and comparatively often return parameter values."))
     aux3 = insFld(aux2, gFld("<strong>resolve-[noun]</strong>"))
       insDoc(aux3, gFld("These methods often have parameters, contain loops, use local variables, create objects, do runtime type-checking or casting, have branches and have multiple return points."))
  aux1 = insFld(foldersTree, gFld("<strong>run-*</strong>"))
    insDoc(aux1, gFld("These methods often read from fields, contain loops, use local variables and have branches. They never return parameter values, and very rarely call themselves recursively, and rarely call other methods with the same name, are static, write parameter values to fields or return field values."))
   aux2 = insFld(aux1, gFld("<strong>run</strong>"))
     insDoc(aux2, gFld("These methods often read from fields, contain loops, use local variables and have branches. They never return parameter values, and very rarely are static or call themselves recursively, and rarely call other methods with the same name, write parameter values to fields or return field values."))
  aux1 = insFld(foldersTree, gFld("<strong>save-*</strong>"))
    insDoc(aux1, gFld("These methods often contain loops, use local variables, create objects, do runtime type-checking or casting and have branches. They very rarely return parameter values, and rarely write parameter values to fields."))
  aux1 = insFld(foldersTree, gFld("<strong>send-*</strong>"))
    insDoc(aux1, gFld("These methods often contain loops, use local variables, create objects, throw exceptions and have branches. They rarely return field values or call themselves recursively."))
    aux2 = insFld(aux1, gFld("<strong>send-[noun]-*</strong>"))
      insDoc(aux2, gFld("These methods often contain loops, use local variables, create objects, throw exceptions and have branches. They rarely return field values, return parameter values or call themselves recursively."))
  aux1 = insFld(foldersTree, gFld("<strong>set-*</strong>"))
    insDoc(aux1, gFld("These methods often have parameters, return void, write to fields and write parameter values to fields. They very rarely return field values, return parameter values or call themselves recursively, and rarely have multiple return points. The phrase appears in all applications."))
   aux2 = insFld(aux1, gFld("<strong>set</strong>"))
     insDoc(aux2, gFld("These methods very often have parameters, and often throw exceptions and do runtime type-checking or casting. They very rarely call themselves recursively, and rarely return field values or return parameter values."))
    aux2 = insFld(aux1, gFld("<strong>set-[verb]-*</strong>"))
      insDoc(aux2, gFld("These methods often have parameters, return void, write to fields and write parameter values to fields. They never call themselves recursively, and very rarely return field values or return parameter values, and rarely call other methods with the same name, contain loops, use local variables, do runtime type-checking or casting or have multiple return points. The phrase appears in most applications."))
     aux3 = insFld(aux2, gFld("<strong>set-[verb]</strong>"))
       insDoc(aux3, gFld("These methods very often return void, and often have parameters, write to fields and write parameter values to fields. They never return field values, return parameter values or call themselves recursively, and rarely call other methods with the same name, contain loops, use local variables, do runtime type-checking or casting or have multiple return points."))
      aux3 = insFld(aux2, gFld("<strong>set-[verb]-[noun]-*</strong>"))
        insDoc(aux3, gFld("These methods often have parameters, return void, write to fields and write parameter values to fields. They never call themselves recursively, and very rarely return field values or return parameter values, and rarely call other methods with the same name, contain loops, use local variables, do runtime type-checking or casting or have multiple return points."))
       aux4 = insFld(aux3, gFld("<strong>set-[verb]-[noun]</strong>"))
         insDoc(aux4, gFld("These methods often have parameters, return void, write to fields and write parameter values to fields. They never call themselves recursively, and very rarely return field values or return parameter values, and rarely call other methods with the same name, contain loops, use local variables, do runtime type-checking or casting or have multiple return points."))
      aux3 = insFld(aux2, gFld("<strong>set-[verb]-[adjective]-*</strong>"))
        insDoc(aux3, gFld("These methods very often write to fields, and often have parameters, return void and write parameter values to fields. They never return parameter values or call themselves recursively, and very rarely contain loops, return field values or have multiple return points, and rarely call other methods with the same name, use local variables, create objects, throw exceptions or do runtime type-checking or casting."))
    aux2 = insFld(aux1, gFld("<strong>set-max-*</strong>"))
      insDoc(aux2, gFld("These methods often have parameters, return void, write to fields and write parameter values to fields. They never return parameter values, and very rarely return field values or have multiple return points, and rarely contain loops, use local variables, do runtime type-checking or casting, are static or call themselves recursively."))
      aux3 = insFld(aux2, gFld("<strong>set-max-[noun]-*</strong>"))
        insDoc(aux3, gFld("These methods often have parameters, return void, write to fields and write parameter values to fields. They never return parameter values, and very rarely return field values or have multiple return points, and rarely contain loops, use local variables, do runtime type-checking or casting or call themselves recursively."))
    aux2 = insFld(aux1, gFld("<strong>set-name-*</strong>"))
      insDoc(aux2, gFld("These methods very often have parameters and return void, and often write to fields and write parameter values to fields. They never call themselves recursively, and very rarely contain loops, are static, return field values or return parameter values, and rarely call other methods with the same name, use local variables or have multiple return points."))
     aux3 = insFld(aux2, gFld("<strong>set-name</strong>"))
       insDoc(aux3, gFld("These methods very often have parameters and return void, and often write to fields and write parameter values to fields. They never call themselves recursively, and very rarely contain loops, are static, return field values or return parameter values, and rarely call other methods with the same name or have multiple return points."))
    aux2 = insFld(aux1, gFld("<strong>set-property-*</strong>"))
      insDoc(aux2, gFld("These methods very often have parameters, and often write to fields. They never return field values, and very rarely call themselves recursively, and rarely return parameter values."))
     aux3 = insFld(aux2, gFld("<strong>set-property</strong>"))
       insDoc(aux3, gFld("These methods always have parameters, and often write to fields and do runtime type-checking or casting. They never return field values or call themselves recursively, and rarely return parameter values."))
    aux2 = insFld(aux1, gFld("<strong>set-target-*</strong>"))
      insDoc(aux2, gFld("These methods often have parameters, return void, write to fields and write parameter values to fields. They never return field values or return parameter values, and rarely call other methods with the same name, are static or call themselves recursively."))
    aux2 = insFld(aux1, gFld("<strong>set-type-*</strong>"))
      insDoc(aux2, gFld("These methods very often have parameters and return void, and often write to fields and write parameter values to fields. They never return field values, and very rarely contain loops, are static or return parameter values, and rarely call other methods with the same name, use local variables, do runtime type-checking or casting, call themselves recursively or have multiple return points."))
     aux3 = insFld(aux2, gFld("<strong>set-type</strong>"))
       insDoc(aux3, gFld("These methods always have parameters, and very often return void and write to fields, and often write parameter values to fields. They never are static, return field values or return parameter values, and very rarely contain loops, and rarely call other methods with the same name, use local variables, do runtime type-checking or casting, call themselves recursively or have multiple return points."))
    aux2 = insFld(aux1, gFld("<strong>set-value-*</strong>"))
      insDoc(aux2, gFld("These methods very often have parameters, and often write to fields, throw exceptions, do runtime type-checking or casting and write parameter values to fields. They very rarely call themselves recursively, and rarely are static, return field values or return parameter values."))
     aux3 = insFld(aux2, gFld("<strong>set-value</strong>"))
       insDoc(aux3, gFld("These methods very often have parameters, and often write to fields, create objects, throw exceptions, do runtime type-checking or casting and write parameter values to fields. They very rarely are static or call themselves recursively, and rarely contain loops, return field values or return parameter values."))
    aux2 = insFld(aux1, gFld("<strong>set-[/noun]-*</strong>"))
      insDoc(aux2, gFld("These methods often have parameters, return void, write to fields and write parameter values to fields. They very rarely return field values, return parameter values or call themselves recursively, and rarely have multiple return points. The phrase appears in practically all applications."))
    aux2 = insFld(aux1, gFld("<strong>set-default-*</strong>"))
      insDoc(aux2, gFld("These methods often return void, write to fields and write parameter values to fields, and comparatively often are static. They very rarely return field values, and rarely call other methods with the same name, return parameter values, call themselves recursively or have multiple return points."))
      aux3 = insFld(aux2, gFld("<strong>set-default-[noun]-*</strong>"))
        insDoc(aux3, gFld("These methods often return void, write to fields and write parameter values to fields, and comparatively often are static. They very rarely return field values, and rarely call other methods with the same name, return parameter values, call themselves recursively or have multiple return points."))
       aux4 = insFld(aux3, gFld("<strong>set-default-[noun]</strong>"))
         insDoc(aux4, gFld("These methods often return void, write to fields and write parameter values to fields, and comparatively often are static. They very rarely return field values, and rarely call other methods with the same name, call themselves recursively or have multiple return points."))
    aux2 = insFld(aux1, gFld("<strong>set-[/adjective]-*</strong>"))
      insDoc(aux2, gFld("These methods often have parameters, return void, write to fields and write parameter values to fields. They very rarely return field values or return parameter values, and rarely call themselves recursively or have multiple return points. The phrase appears in most applications."))
    aux2 = insFld(aux1, gFld("<strong>set-[preposition]-*</strong>"))
      insDoc(aux2, gFld("These methods often write to fields and write parameter values to fields. They very rarely return field values or call themselves recursively, and rarely are static, return parameter values or have multiple return points."))
    aux2 = insFld(aux1, gFld("<strong>set-[type]-*</strong>"))
      insDoc(aux2, gFld("These methods often have parameters, return void, write to fields and write parameter values to fields. They very rarely return field values, and rarely return parameter values or call themselves recursively. The phrase appears in most applications."))
     aux3 = insFld(aux2, gFld("<strong>set-[type]</strong>"))
       insDoc(aux3, gFld("These methods often have parameters, return void, write to fields and write parameter values to fields. They very rarely return field values or return parameter values, and rarely call themselves recursively or have multiple return points. The phrase appears in most applications."))
  aux1 = insFld(foldersTree, gFld("<strong>start-*</strong>"))
    insDoc(aux1, gFld("These methods often write to fields, create objects and have branches. They very rarely call themselves recursively, and rarely return field values or return parameter values. The phrase appears in most applications."))
   aux2 = insFld(aux1, gFld("<strong>start</strong>"))
     insDoc(aux2, gFld("These methods often write to fields, call other methods with the same name, create objects, throw exceptions and have branches. They never return parameter values, and very rarely call themselves recursively, and rarely return field values."))
    aux2 = insFld(aux1, gFld("<strong>start-[noun]-*</strong>"))
      insDoc(aux2, gFld("These methods often write to fields and create objects. They very rarely return field values or call themselves recursively, and rarely return parameter values."))
     aux3 = insFld(aux2, gFld("<strong>start-[noun]</strong>"))
       insDoc(aux3, gFld("These methods often write to fields, create objects and have branches. They very rarely return field values or call themselves recursively, and rarely return parameter values."))
  aux1 = insFld(foldersTree, gFld("<strong>stop-*</strong>"))
    insDoc(aux1, gFld("These methods often write to fields and have branches. They very rarely return field values, and rarely write parameter values to fields, return parameter values or call themselves recursively."))
  aux1 = insFld(foldersTree, gFld("<strong>update-*</strong>"))
    insDoc(aux1, gFld("These methods often contain loops, use local variables, do runtime type-checking or casting and have branches. They very rarely return field values, and rarely return parameter values."))
    aux2 = insFld(aux1, gFld("<strong>update-[noun]-*</strong>"))
      insDoc(aux2, gFld("These methods often contain loops, use local variables, do runtime type-checking or casting and have branches. They very rarely return field values, and rarely return parameter values."))
     aux3 = insFld(aux2, gFld("<strong>update-[noun]</strong>"))
       insDoc(aux3, gFld("These methods often contain loops, use local variables, do runtime type-checking or casting and have branches. They very rarely return field values, and rarely are static or return parameter values."))
  aux1 = insFld(foldersTree, gFld("<strong>validate-*</strong>"))
    insDoc(aux1, gFld("These methods often contain loops, use local variables, create objects, throw exceptions and have branches. They rarely write parameter values to fields, return field values or call themselves recursively."))
  aux1 = insFld(foldersTree, gFld("<strong>write-*</strong>"))
    insDoc(aux1, gFld("These methods often have parameters, contain loops and are static. They very rarely return field values, and rarely write parameter values to fields or return parameter values. The phrase appears in most applications."))
   aux2 = insFld(aux1, gFld("<strong>write</strong>"))
     insDoc(aux2, gFld("These methods often have parameters, return void, call other methods with the same name, contain loops and are static. They very rarely return field values or return parameter values, and rarely write parameter values to fields."))
    aux2 = insFld(aux1, gFld("<strong>write-[noun]-*</strong>"))
      insDoc(aux2, gFld("These methods often have parameters, contain loops, use local variables and have branches. They very rarely return field values, and rarely write parameter values to fields."))
     aux3 = insFld(aux2, gFld("<strong>write-[noun]</strong>"))
       insDoc(aux3, gFld("These methods often have parameters and contain loops. They very rarely return field values, and rarely write parameter values to fields."))
      aux3 = insFld(aux2, gFld("<strong>write-[noun]-[noun]-*</strong>"))
        insDoc(aux3, gFld("These methods often have parameters, contain loops, use local variables, create objects and have branches. They very rarely return field values, and rarely call other methods with the same name, write parameter values to fields, return parameter values or call themselves recursively."))
       aux4 = insFld(aux3, gFld("<strong>write-[noun]-[noun]</strong>"))
         insDoc(aux4, gFld("These methods often contain loops, use local variables, create objects and have branches. They very rarely return field values, and rarely call other methods with the same name, write parameter values to fields, return parameter values or call themselves recursively."))
    aux2 = insFld(aux1, gFld("<strong>write-[adjective]-*</strong>"))
      insDoc(aux2, gFld("These methods often contain loops, use local variables and have branches, and comparatively often are static. They never return field values, and rarely return parameter values or call themselves recursively."))
    aux2 = insFld(aux1, gFld("<strong>write-[type]-*</strong>"))
      insDoc(aux2, gFld("These methods often have parameters, call other methods with the same name, contain loops, use local variables, do runtime type-checking or casting and have branches. They very rarely return field values, and rarely write parameter values to fields."))
     aux3 = insFld(aux2, gFld("<strong>write-[type]</strong>"))
       insDoc(aux3, gFld("These methods often have parameters, call other methods with the same name, contain loops, use local variables, do runtime type-checking or casting and have branches. They very rarely return field values, and rarely write parameter values to fields."))
  aux1 = insFld(foldersTree, gFld("<strong>[/verb]-*</strong>"))
    insDoc(aux1, gFld("These methods often contain loops, use local variables and have branches, and comparatively often are static. The phrase appears in all applications."))
  aux1 = insFld(foldersTree, gFld("<strong>error-*</strong>"))
    insDoc(aux1, gFld("These methods often have parameters and are static. They never call themselves recursively, and very rarely return parameter values, and rarely contain loops, do runtime type-checking or casting, return field values or have multiple return points."))
   aux2 = insFld(aux1, gFld("<strong>error</strong>"))
     insDoc(aux2, gFld("These methods often have parameters, return void and throw exceptions, and comparatively often are static. They never return parameter values or call themselves recursively, and rarely contain loops, do runtime type-checking or casting, return field values or have multiple return points."))
  aux1 = insFld(foldersTree, gFld("<strong>index-*</strong>"))
    insDoc(aux1, gFld("These methods very often contain loops and have multiple return points, and often have parameters, call other methods with the same name, use local variables, are static and have branches, and comparatively often return parameter values. They rarely write to fields, write parameter values to fields or call themselves recursively."))
  aux1 = insFld(foldersTree, gFld("<strong>iterator-*</strong>"))
    insDoc(aux1, gFld("These methods often create objects. They never write parameter values to fields or call themselves recursively, and very rarely return void or write to fields, and rarely have parameters, contain loops, use local variables, do runtime type-checking or casting, return field values, return parameter values, have branches or have multiple return points."))
   aux2 = insFld(aux1, gFld("<strong>iterator</strong>"))
     insDoc(aux2, gFld("These methods often call other methods with the same name and create objects. They never return void, write parameter values to fields or call themselves recursively, and very rarely write to fields or return parameter values, and rarely have parameters, contain loops, use local variables, do runtime type-checking or casting, return field values, have branches or have multiple return points."))
  aux1 = insFld(foldersTree, gFld("<strong>key-*</strong>"))
    insDoc(aux1, gFld("These methods never call themselves recursively, and very rarely return parameter values, and rarely write parameter values to fields."))
  aux1 = insFld(foldersTree, gFld("<strong>setup-*</strong>"))
    insDoc(aux1, gFld("These methods often write to fields, contain loops, use local variables, create objects and have branches. They rarely call other methods with the same name or return field values."))
  aux1 = insFld(foldersTree, gFld("<strong>size-*</strong>"))
    insDoc(aux1, gFld("These methods very rarely return void, write to fields, do runtime type-checking or casting, write parameter values to fields, return parameter values or call themselves recursively, and rarely have parameters, contain loops, create objects, are static, have branches or have multiple return points."))
   aux2 = insFld(aux1, gFld("<strong>size</strong>"))
     insDoc(aux2, gFld("These methods often read from fields and call other methods with the same name. They never return void, return parameter values or call themselves recursively, and very rarely write to fields or write parameter values to fields, and rarely have parameters, use local variables, create objects or do runtime type-checking or casting."))
  aux1 = insFld(foldersTree, gFld("<strong>value-*</strong>"))
    insDoc(aux1, gFld("These methods often do runtime type-checking or casting and are static. They rarely write parameter values to fields, return parameter values or call themselves recursively."))
  aux1 = insFld(foldersTree, gFld("<strong>[/noun]-*</strong>"))
    insDoc(aux1, gFld("These methods comparatively often are static. The phrase appears in all applications."))
  aux1 = insFld(foldersTree, gFld("<strong>fatal-*</strong>"))
    insDoc(aux1, gFld("These methods very often have parameters and throw exceptions, and often return void. They never contain loops, return field values, return parameter values or call themselves recursively, and very rarely do runtime type-checking or casting or have multiple return points."))
  aux1 = insFld(foldersTree, gFld("<strong>last-*</strong>"))
    insDoc(aux1, gFld("These methods often call other methods with the same name, contain loops and have multiple return points, and comparatively often are static. They rarely return void or write parameter values to fields."))
  aux1 = insFld(foldersTree, gFld("<strong>main-*</strong>"))
    insDoc(aux1, gFld("These methods very often use local variables, create objects and are static, and often have parameters, return void, contain loops and have branches. They never return parameter values or call themselves recursively, and very rarely write parameter values to fields or return field values, and rarely write to fields or call other methods with the same name."))
   aux2 = insFld(aux1, gFld("<strong>main</strong>"))
     insDoc(aux2, gFld("These methods very often have parameters, return void, use local variables, create objects and are static, and often contain loops and have branches. They never return parameter values or call themselves recursively, and very rarely write parameter values to fields or return field values, and rarely write to fields or call other methods with the same name."))
  aux1 = insFld(foldersTree, gFld("<strong>new-*</strong>"))
    insDoc(aux1, gFld("These methods very often are static, and often call other methods with the same name, create objects and do runtime type-checking or casting. They very rarely call themselves recursively, and rarely return void, contain loops, write parameter values to fields, return field values, return parameter values or have multiple return points."))
    aux2 = insFld(aux1, gFld("<strong>new-[noun]-*</strong>"))
      insDoc(aux2, gFld("These methods very often are static, and often call other methods with the same name, create objects and do runtime type-checking or casting. They very rarely call themselves recursively, and rarely return void, contain loops, write parameter values to fields, return field values or return parameter values."))
     aux3 = insFld(aux2, gFld("<strong>new-[noun]</strong>"))
       insDoc(aux3, gFld("These methods very often are static, and often call other methods with the same name, create objects and do runtime type-checking or casting. They very rarely call themselves recursively, and rarely return void, contain loops, write parameter values to fields, return field values, return parameter values, have branches or have multiple return points."))
    aux2 = insFld(aux1, gFld("<strong>new-[type]-*</strong>"))
      insDoc(aux2, gFld("These methods very often create objects. They very rarely return void, return field values, return parameter values or call themselves recursively, and rarely write to fields, read from fields, contain loops, write parameter values to fields or have multiple return points."))
     aux3 = insFld(aux2, gFld("<strong>new-[type]</strong>"))
       insDoc(aux3, gFld("These methods very often create objects. They very rarely return void, return field values, return parameter values or call themselves recursively, and rarely write to fields, read from fields, contain loops, write parameter values to fields or have multiple return points."))
  aux1 = insFld(foldersTree, gFld("<strong>next-*</strong>"))
    insDoc(aux1, gFld("These methods often write to fields, read from fields, contain loops, throw exceptions, have branches and have multiple return points. They rarely write parameter values to fields."))
   aux2 = insFld(aux1, gFld("<strong>next</strong>"))
     insDoc(aux2, gFld("These methods often write to fields, read from fields, throw exceptions and have multiple return points, and comparatively often contain loops. They never write parameter values to fields, and rarely have parameters, return void, are static or return parameter values."))
    aux2 = insFld(aux1, gFld("<strong>next-[noun]-*</strong>"))
      insDoc(aux2, gFld("These methods often write to fields, read from fields, contain loops, throw exceptions, have branches and have multiple return points. They rarely write parameter values to fields."))
     aux3 = insFld(aux2, gFld("<strong>next-[noun]</strong>"))
       insDoc(aux3, gFld("These methods often write to fields, read from fields, contain loops, create objects, throw exceptions, have branches and have multiple return points. They rarely write parameter values to fields."))
  aux1 = insFld(foldersTree, gFld("<strong>warning-*</strong>"))
    insDoc(aux1, gFld("These methods often have parameters and return void, and comparatively often are static. They never return parameter values or call themselves recursively, and very rarely return field values, and rarely contain loops, do runtime type-checking or casting or have multiple return points."))
   aux2 = insFld(aux1, gFld("<strong>warning</strong>"))
     insDoc(aux2, gFld("These methods very often have parameters, and often return void, and comparatively often are static. They never return parameter values or call themselves recursively, and very rarely return field values, and rarely contain loops, do runtime type-checking or casting or have multiple return points."))
  aux1 = insFld(foldersTree, gFld("<strong>[/adjective]-*</strong>"))
    insDoc(aux1, gFld("These methods comparatively often are static. The phrase appears in most applications."))
  aux1 = insFld(foldersTree, gFld("<strong>[adverb]-*</strong>"))
    insDoc(aux1, gFld("These methods often have branches and have multiple return points, and comparatively often contain loops and are static. They rarely write parameter values to fields."))
  aux1 = insFld(foldersTree, gFld("<strong>to-*</strong>"))
    insDoc(aux1, gFld("These methods often call other methods with the same name, contain loops and create objects, and comparatively often are static. They very rarely write parameter values to fields, and rarely return void, write to fields or call themselves recursively. The phrase appears in all applications."))
    aux2 = insFld(aux1, gFld("<strong>to-[noun]-*</strong>"))
      insDoc(aux2, gFld("These methods often contain loops, use local variables, create objects and are static, and comparatively often return parameter values. They very rarely write parameter values to fields, and rarely write to fields."))
     aux3 = insFld(aux2, gFld("<strong>to-[noun]</strong>"))
       insDoc(aux3, gFld("These methods often call other methods with the same name, contain loops, use local variables and are static, and comparatively often return parameter values. They very rarely write parameter values to fields, and rarely return void or write to fields."))
      aux3 = insFld(aux2, gFld("<strong>to-[noun]-[noun]-*</strong>"))
        insDoc(aux3, gFld("These methods often contain loops, use local variables, create objects and are static, and comparatively often return parameter values. They never write parameter values to fields, and rarely write to fields."))
       aux4 = insFld(aux3, gFld("<strong>to-[noun]-[noun]</strong>"))
         insDoc(aux4, gFld("These methods often contain loops, use local variables, create objects and are static, and comparatively often return parameter values. They never write parameter values to fields, and rarely write to fields."))
    aux2 = insFld(aux1, gFld("<strong>to-[adjective]-*</strong>"))
      insDoc(aux2, gFld("These methods often contain loops, use local variables, create objects, are static, have branches and have multiple return points, and comparatively often return parameter values. They rarely write to fields, write parameter values to fields or return field values."))
    aux2 = insFld(aux1, gFld("<strong>to-String-*</strong>"))
      insDoc(aux2, gFld("These methods very often call other methods with the same name and create objects. They very rarely write parameter values to fields or call themselves recursively, and rarely have parameters, return void, write to fields, throw exceptions or return parameter values. The phrase appears in practically all applications."))
     aux3 = insFld(aux2, gFld("<strong>to-String</strong>"))
       insDoc(aux3, gFld("These methods very often call other methods with the same name and create objects. They very rarely write parameter values to fields or call themselves recursively, and rarely have parameters, return void, write to fields, throw exceptions or return parameter values. The phrase appears in practically all applications."))
    aux2 = insFld(aux1, gFld("<strong>to-[/type]-*</strong>"))
      insDoc(aux2, gFld("These methods often contain loops, use local variables, create objects, do runtime type-checking or casting, are static, have branches and have multiple return points. They never write parameter values to fields, and rarely return void."))
  aux1 = insFld(foldersTree, gFld("<strong>[/preposition]-*</strong>"))
    insDoc(aux1, gFld("These methods often are static. They rarely call themselves recursively. The phrase appears in most applications."))
  aux1 = insFld(foldersTree, gFld("<strong>[pronoun]-*</strong>"))
    insDoc(aux1, gFld("These methods often contain loops and are static. They rarely write parameter values to fields."))
    aux2 = insFld(aux1, gFld("<strong>[pronoun]-[noun]-*</strong>"))
      insDoc(aux2, gFld("These methods often contain loops and are static. They rarely write parameter values to fields."))
  aux1 = insFld(foldersTree, gFld("<strong>[type]-*</strong>"))
    insDoc(aux1, gFld("These methods often are static."))
